# InvoiceShelf 邮件通知链路梳理

## 一、业务事件与邮件类型总览

| 序号 | 邮件类型 | Mailable 类 | 触发场景 |
|------|----------|-------------|----------|
| 1 | 发票发送邮件 | `SendInvoiceMail` | 管理员发送发票 / 周期性发票自动发送 |
| 2 | 估价单发送邮件 | `SendEstimateMail` | 管理员发送估价单 |
| 3 | 付款发送邮件 | `SendPaymentMail` | 管理员发送付款凭证 |
| 4 | 发票被查看通知 | `InvoiceViewedMail` | 客户查看发票 PDF 时 |
| 5 | 估价单被查看通知 | `EstimateViewedMail` | 客户查看估价单 PDF 时 |
| 6 | 测试邮件 | `TestMail` | 管理员测试邮件配置 |
| 7 | 管理员重置密码 | `MailResetPasswordNotification` | 管理员请求密码重置 |
| 8 | 客户重置密码 | `CustomerMailResetPasswordNotification` | 客户请求密码重置 |

---

## 二、发送类邮件完整链路（以发票为例）

### 2.1 触发入口

#### 入口一：管理员手动发送
- **控制器**: [SendInvoiceController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Admin/Invoice/SendInvoiceController.php)
- **路由**: `POST /api/v1/admin/invoices/{invoice}/send`
- **关键方法**: `__invoke()` → `$invoice->send($request->all())`

#### 入口二：周期性发票自动发送
- **模型**: [RecurringInvoice.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/RecurringInvoice.php)
- **关键方法**: `createInvoice()` 第 379-391 行
- **触发条件**: `$this->send_automatically == true` 时，生成发票后自动调用 `$invoice->send($data)`

### 2.2 模型层数据组装

**文件**: [Invoice.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Invoice.php)

#### 第一步：`send()` 方法（第 475-498 行）
```php
public function send($data)
{
    $data = $this->sendInvoiceData($data);  // 组装数据

    $mail = \Mail::to($data['to']);
    // ... cc / bcc 处理
    $mail->send(new SendInvoiceMail($data));  // 发送邮件

    // 更新状态为 SENT
    if ($this->status == Invoice::STATUS_DRAFT) {
        $this->status = Invoice::STATUS_SENT;
        $this->sent = true;
        $this->save();
    }
}
```

#### 第二步：`sendInvoiceData()` 方法（第 453-463 行）
```php
public function sendInvoiceData($data)
{
    $data['invoice'] = $this->toArray();
    $data['customer'] = $this->customer->toArray();
    $data['company'] = Company::find($this->company_id);
    $data['subject'] = $this->getEmailString($data['subject']);  // 模板变量替换
    $data['body'] = $this->getEmailString($data['body']);        // 模板变量替换
    $data['attach']['data'] = ($this->getEmailAttachmentSetting()) 
        ? $this->getPDFData() : null;

    return $data;
}
```

### 2.3 动态数据模板替换（变量替换）

**核心方法**: `getEmailString()` → 位于 [Invoice.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Invoice.php) 第 662-669 行

```php
public function getEmailString($body)
{
    $values = array_merge($this->getFieldsArray(), $this->getExtraFields());
    $body = strtr($body, $values);
    return preg_replace('/{(.*?)}/', '', $body);  // 清除未匹配的占位符
}
```

#### 变量来源一：`getFieldsArray()`
**Trait**: [GeneratesPdfTrait.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Traits/GeneratesPdfTrait.php) 第 112-170 行

包含三类字段：
1. **客户地址字段** - 收货地址、账单地址（如 `{SHIPPING_ADDRESS_NAME}`, `{BILLING_CITY}`）
2. **公司字段** - 公司信息（如 `{COMPANY_NAME}`, `{COMPANY_PHONE}`）
3. **联系人字段** - 客户联系人信息（如 `{CONTACT_DISPLAY_NAME}`, `{CONTACT_EMAIL}`）
4. **自定义字段** - 模型和客户的自定义字段（动态生成）

#### 变量来源二：`getExtraFields()`
**文件**: [Invoice.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Invoice.php) 第 671-679 行

```php
public function getExtraFields()
{
    return [
        '{INVOICE_DATE}' => $this->formattedInvoiceDate,
        '{INVOICE_DUE_DATE}' => $this->formattedDueDate,
        '{INVOICE_NUMBER}' => $this->invoice_number,
        '{INVOICE_REF_NUMBER}' => $this->reference_number,
    ];
}
```

**替换原理**: 使用 PHP `strtr()` 函数进行字符串替换，将形如 `{INVOICE_NUMBER}` 的占位符替换为实际数据。

### 2.4 Mailable 构建

**文件**: [SendInvoiceMail.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/SendInvoiceMail.php)

#### `build()` 方法（第 34-64 行）执行步骤：
1. **创建邮件日志** (`EmailLog`) - 记录发件人、收件人、主题、正文等
2. **生成访问 Token** - 通过 Hashids 编码生成邮件日志的唯一 token
3. **生成访问 URL** - `route('invoice', ['email_log' => $log->token])`
4. **设置邮件属性** - from / subject
5. **渲染 Markdown 视图** - `markdown('emails.send.invoice', ['data', $this->data])`
6. **添加 PDF 附件**（如需） - `attachData($pdf->output(), 'xxx.pdf')`

### 2.5 视图模板渲染

**模板路径**: `resources/views/emails/send/invoice.blade.php`

**文件**: [invoice.blade.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/resources/views/emails/send/invoice.blade.php)

使用 Laravel Markdown 邮件组件：
- `@component('mail::layout')` - 邮件布局
- `@slot('header')` - 头部（公司 Logo / 名称）
- `@slot('subcopy')` - 正文区域，包含：
  - `{!! $data['body'] !!}` - 已替换变量的邮件正文（HTML）
  - 查看按钮（无附件时显示）
- `@slot('footer')` - 页脚

---

## 三、查看通知类邮件链路（以发票为例）

### 3.1 触发入口

**控制器**: [InvoicePdfController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Customer/InvoicePdfController.php)

**方法**: `getPdf()` 第 16-53 行

### 3.2 触发条件判断
```php
if ($invoice && ($invoice->status == Invoice::STATUS_SENT || $invoice->status == Invoice::STATUS_DRAFT)) {
    $invoice->status = Invoice::STATUS_VIEWED;  // 更新状态为已查看
    $invoice->viewed = true;
    $invoice->save();

    $notifyInvoiceViewed = CompanySetting::getSetting('notify_invoice_viewed', $invoice->company_id);

    if ($notifyInvoiceViewed == 'YES') {  // 需开启通知设置
        // 组装数据并发送邮件
        $data['invoice'] = $invoice->toArray();
        $data['user'] = Customer::find($invoice->customer_id)->toArray();
        $notificationEmail = CompanySetting::getSetting('notification_email', $invoice->company_id);
        
        \Mail::to($notificationEmail)->send(new InvoiceViewedMail($data));
    }
}
```

### 3.3 Mailable 与视图

**Mailable**: [InvoiceViewedMail.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/InvoiceViewedMail.php)

**视图**: [viewed/invoice.blade.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/resources/views/emails/viewed/invoice.blade.php)

使用 `@component('mail::message')` 简化布局，包含：
- 通知文案：`@lang('mail_viewed_invoice', ['name' => $data['user']['name']])`
- 查看按钮：跳转到后台发票详情页
- 签名：`{{ config('app.name') }}`

---

## 四、其他邮件类型

### 4.1 测试邮件
- **触发**: [MailConfigurationController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Admin/Settings/MailConfigurationController.php) `testEmailConfig()`
- **Mailable**: [TestMail.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/TestMail.php)
- **视图**: [test.blade.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/resources/views/emails/test.blade.php)

### 4.2 重置密码通知
- **管理员**: [MailResetPasswordNotification.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Notifications/MailResetPasswordNotification.php)
- **客户**: [CustomerMailResetPasswordNotification.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Notifications/CustomerMailResetPasswordNotification.php)
- 继承自 `Illuminate\Auth\Notifications\ResetPassword`
- 使用 `MailMessage` 构建，不使用自定义 Blade 模板

---

## 五、完整流程图

```
管理员API / 定时任务
        │
        ▼
  控制器接收请求
        │
        ▼
  模型 send() 方法
        │
        ├─► sendXxxData() 组装数据
        │       │
        │       ├─► 模型数据 toArray()
        │       ├─► 客户/公司数据
        │       └─► getEmailString() 变量替换
        │              │
        │              ├─► getFieldsArray() （地址/公司/联系人/自定义字段）
        │              └─► getExtraFields() （业务特定字段）
        │
        ├─► Mail::to()->send(new Mailable($data))
        │
        ▼
  Mailable::build()
        │
        ├─► 创建 EmailLog（含追踪 token）
        ├─► 生成访问 URL
        ├─► 设置 from / subject
        ├─► markdown() 渲染 Blade 视图
        └─► attachData() 添加 PDF 附件（可选）
        │
        ▼
  Blade 视图渲染
        │
        ├─► mail::layout 布局
        ├─► 头部（Logo/公司名）
        ├─► 正文（已替换变量的 body）
        ├─► 查看按钮（无附件时）
        └─► 页脚
        │
        ▼
     邮件发送
```

---

## 六、关键技术点

### 6.1 邮件追踪机制
- 每封发送邮件创建一条 `EmailLog` 记录
- 通过 `Hashids` 编码 ID 生成 token，用于 URL 访问追踪
- 客户点击链接访问 PDF 时，通过 `EmailLog` 解析并更新查看状态

### 6.2 变量替换机制
- 使用 **占位符语法**: `{FIELD_NAME}`
- **替换函数**: PHP 原生 `strtr()`
- **兜底处理**: `preg_replace('/{(.*?)}/', '', $body)` 清除未匹配占位符
- **字段来源**: `getFieldsArray()`（通用字段）+ `getExtraFields()`（业务特有字段）

### 6.3 邮件模板体系
- **发送类邮件**: 使用 `mail::layout` 完整布局，带 Logo、按钮、页脚
- **通知类邮件**: 使用 `mail::message` 简化布局
- **Markdown 驱动**: 所有邮件均使用 `markdown()` 方法渲染

### 6.4 PDF 附件
- 通过 `getEmailAttachmentSetting()` 读取公司设置决定是否附加
- PDF 数据由 `getPDFData()` 生成，使用 PDF 驱动（如 DomPDF / Gotenberg）

---

## 七、邮件队列异步发送策略深度分析

### 7.1 队列驱动配置

**配置文件**: [queue.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/config/queue.php)

- **默认队列驱动**: `sync`（同步执行）
- **环境变量**: `QUEUE_CONNECTION` 控制默认驱动，默认值 `sync`
- **可用驱动**: `sync`, `database`, `beanstalkd`, `sqs`, `redis`

```php
'default' => env('QUEUE_CONNECTION', 'sync'),
```

### 7.2 Mailable 的 Queueable Trait

所有 6 个 Mailable 类都引入了 `Queueable` trait：

| Mailable 类 | 位置 |
|------------|------|
| `SendInvoiceMail` | [第 14 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/SendInvoiceMail.php#L14-L15) |
| `SendEstimateMail` | [第 14 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/SendEstimateMail.php#L14-L15) |
| `SendPaymentMail` | [第 14 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/SendPaymentMail.php#L14-L15) |
| `InvoiceViewedMail` | [第 11 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/InvoiceViewedMail.php#L11-L12) |
| `EstimateViewedMail` | [第 11 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/EstimateViewedMail.php#L11-L12) |
| `TestMail` | [第 11 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/TestMail.php#L11-L12) |

> **注意**: `Queueable` trait 只是提供了队列配置能力（如 `onQueue()`, `delay()` 等链式调用方法），但并不会自动将邮件推入队列。是否入队取决于**调用方式**。

### 7.3 发送方式：同步 vs 异步

**实际调用方式**: 全部使用 `Mail::to()->send()` 同步发送

| 触发位置 | 代码位置 | 调用方式 |
|---------|---------|---------|
| 发票发送 | [Invoice.php 第 479-486 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Invoice.php#L479-L486) | `$mail->send(new SendInvoiceMail($data))` |
| 估价单发送 | [Estimate.php 第 377-384 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Estimate.php#L377-L384) | `$mail->send(new SendEstimateMail($data))` |
| 付款发送 | [Payment.php 第 147-154 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Payment.php#L147-L154) | `$mail->send(new SendPaymentMail($data))` |
| 发票被查看 | [InvoicePdfController.php 第 38 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Customer/InvoicePdfController.php#L38-L38) | `\Mail::to(...)->send(new InvoiceViewedMail($data))` |
| 估价单被查看 | [EstimatePdfController.php 第 37 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Customer/EstimatePdfController.php#L37-L37) | `\Mail::to(...)->send(new EstimateViewedMail($data))` |
| 测试邮件 | [MailConfigurationController.php 第 251 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Admin/Settings/MailConfigurationController.php#L251-L251) | `Mail::to(...)->send(new TestMail(...))` |

**结论**: 当前所有邮件均为**同步发送**，未使用 `Mail::queue()` 或 `Mail::later()` 推入队列。

### 7.4 失败重试机制

#### 同步模式（sync）
- 默认 `sync` 驱动下，邮件发送失败会直接抛出异常
- 无自动重试机制
- 失败信息体现在当前请求的响应中

#### 异步队列模式（如 database/redis）
若切换为异步驱动，失败重试由以下配置控制：

**失败任务存储**: [queue.php 第 106-110 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/config/queue.php#L106-L110)
- 默认驱动: `database-uuids`
- 存储表: `failed_jobs`
- 数据库连接: 与默认 `DB_CONNECTION` 一致（默认 `sqlite`）

**重试超时**: 各驱动的 `retry_after` 配置（默认 90 秒）
- database: `DB_QUEUE_RETRY_AFTER` 环境变量
- beanstalkd: `BEANSTALKD_QUEUE_RETRY_AFTER` 环境变量
- redis: `REDIS_QUEUE_RETRY_AFTER` 环境变量

#### Mailable 自定义重试
所有 Mailable 类均未设置 `$tries` 属性或 `retryUntil()` 方法，使用 Laravel 默认重试策略。

### 7.5 通知类邮件的队列情况

两个重置密码通知类也使用了 `Queueable` trait：

- [MailResetPasswordNotification.php 第 12 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Notifications/MailResetPasswordNotification.php#L12-L12)
- [CustomerMailResetPasswordNotification.php 第 12 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Notifications/CustomerMailResetPasswordNotification.php#L12-L12)

但由于通过 `$user->notify()` 调用且未实现 `ShouldQueue` 接口，仍为同步执行。

---

## 八、重置密码邮件完整链路

### 8.1 管理员重置密码链路

#### 第一步：请求发送重置链接
- **控制器**: [ForgotPasswordController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Admin/Auth/ForgotPasswordController.php)
- **使用 Trait**: `Illuminate\Foundation\Auth\SendsPasswordResetEmails`
- **Broker**: `users`（默认）

**调用链**:
```
POST /api/v1/admin/forgot-password
  → ForgotPasswordController::sendResetLinkEmail()
    → $this->broker()->sendResetLink($credentials)
      → PasswordBroker::sendResetLink()
        → 查找用户
        → 创建 token（PasswordRepository::create()）
        → 用户模型 sendPasswordResetNotification($token)
```

#### 第二步：Token 生成与存储
- **配置**: [auth.php 第 89-94 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/config/auth.php#L89-L94)
  - 表: `password_reset_tokens`
  - 过期时间: 60 分钟
  - 限流: 60 秒

Token 由 Laravel 的 `DatabaseTokenRepository` 生成：
- 使用 `hash_hmac` 生成随机 token
- 存储在 `password_reset_tokens` 表中，包含 email、token（哈希后）、created_at

#### 第三步：通知发送
**模型方法**: [User.php 第 175-177 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/User.php#L175-L177)
```php
public function sendPasswordResetNotification($token)
{
    $this->notify(new MailResetPasswordNotification($token));
}
```

#### 第四步：URL 组装
**通知类**: [MailResetPasswordNotification.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Notifications/MailResetPasswordNotification.php)

`toMail()` 方法（第 39-49 行）:
```php
public function toMail($notifiable): MailMessage
{
    $link = url('/reset-password/'.$this->token);
    return (new MailMessage)
        ->subject('Reset Password Notification')
        ->line('Hello! You are receiving this email because we received a password reset request for your account.')
        ->action('Reset Password', $link)
        ->line('This password reset link will expire in '.config('auth.passwords.users.expire').' minutes')
        ->line('If you did not request a password reset, no further action is required.');
}
```

**URL 格式**: `{APP_URL}/reset-password/{token}`

#### 第五步：重置密码
- **控制器**: [ResetPasswordController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Admin/Auth/ResetPasswordController.php)
- **使用 Trait**: `Illuminate\Foundation\Auth\ResetsPasswords`
- 重置成功后触发 `PasswordReset` 事件

### 8.2 客户重置密码链路

与管理员链路类似，区别在于：

| 差异点 | 管理员 | 客户 |
|-------|--------|------|
| Broker | `users` | `customers` |
| 控制器 | `Admin\Auth\ForgotPasswordController` | `Customer\Auth\ForgotPasswordController` |
| 模型方法 | `User::sendPasswordResetNotification()` | `Customer::sendPasswordResetNotification()` |
| 通知类 | `MailResetPasswordNotification` | `CustomerMailResetPasswordNotification` |
| URL 格式 | `/reset-password/{token}` | `/{company_slug}/customer/reset/password/{token}` |

**客户通知 URL 组装**: [CustomerMailResetPasswordNotification.php 第 41 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Notifications/CustomerMailResetPasswordNotification.php#L41-L41)
```php
$link = url("/{$notifiable->company->slug}/customer/reset/password/".$this->token);
```

### 8.3 重置密码完整流程图

```
用户请求忘记密码
        │
        ▼
  ForgotPasswordController
        │
        ▼
  PasswordBroker::sendResetLink()
        │
        ├─► 通过邮箱查找用户
        ├─► 生成随机 token
        ├─► 哈希 token 并存入 password_reset_tokens 表
        └─► 调用 User/Customer::sendPasswordResetNotification($token)
        │
        ▼
  通知类 toMail() 方法
        │
        ├─► 组装重置 URL（token 作为路径参数）
        ├─► 构建 MailMessage（标题/正文/按钮）
        └─► 通过 mail 通道发送
        │
        ▼
  用户收到邮件 → 点击重置链接
        │
        ▼
  ResetPasswordController
        │
        ├─► 验证 token 有效性
        ├─► 更新用户密码
        ├─► 更新 remember_token
        └─► 触发 PasswordReset 事件
```

---

## 九、邮件多语言切换机制

### 9.1 多语言配置

**支持语言列表**: [invoiceshelf.php 第 53-90 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/config/invoiceshelf.php#L53-L90)

共支持 37 种语言，包括：
- 英语（en）- 默认
- 中文简体（zh_CN）、中文繁体（zh）
- 日语（ja）、韩语（ko）
- 法语（fr）、德语（de）、西班牙语（es）等

语言文件位置: `lang/` 目录
- 翻译文件: `lang/{code}.json`（如 `lang/zh-cn.json`）
- 系统消息: `lang/en/auth.php`, `lang/en/passwords.php` 等

### 9.2 语言设置存储

- **存储位置**: `CompanySetting` 表的 `language` 字段
- **默认值**: `en`
- **设置来源**: 公司偏好设置

### 9.3 邮件相关的语言切换

发送类邮件中，语言切换发生在 **PDF 生成阶段**，而非邮件正文本身：

**发票 PDF**: [Invoice.php 第 585-588 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Invoice.php#L585-L588)
```php
$locale = CompanySetting::getSetting('language', $company->id);
App::setLocale($locale);
```

**估价单 PDF**: [Estimate.php 第 415-418 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Estimate.php#L415-L418)

**付款 PDF**: [Payment.php 第 380-382 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Payment.php#L380-L382)

### 9.4 邮件正文的多语言

邮件正文中使用 `@lang()` 指令进行翻译，例如：

- 发票被查看邮件: `@lang('mail_viewed_invoice', ['name' => $data['user']['name']])`
- 按钮文案: `@lang('mail_view_invoice')`
- 签名: `@lang('mail_thanks')`

这些翻译键定义在 `lang/*.json` 文件中，使用当前应用的 `locale` 进行查找。

> **注意**: 由于邮件发送时未显式设置语言环境（如 `$mail->locale()`），邮件使用的是请求时的系统当前语言。对于由定时任务触发的邮件（如周期性发票），可能使用默认 `APP_LOCALE`。

### 9.5 应用级语言配置

- **默认语言**: `APP_LOCALE` 环境变量，默认 `en`
- **安装时设置**: 通过安装向导选择，存储为 `profile_language`
- **运行时切换**: 各功能模块在需要时通过 `App::setLocale($locale)` 临时切换

---

## 十、PDF 附件驱动选择逻辑

### 10.1 PDF 驱动配置

**配置文件**: [pdf.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/config/pdf.php)

```php
'driver' => env('PDF_DRIVER', 'dompdf'),

'connections' => [
    'dompdf' => [],
    'gotenberg' => [
        'host' => env('GOTENBERG_HOST', 'http://pdf:3000'),
        'papersize' => env('GOTENBERG_PAPERSIZE', '210mm 297mm'),
    ],
],
```

- **默认驱动**: `dompdf`
- **可选驱动**: `dompdf`, `gotenberg`
- **环境变量**: `PDF_DRIVER`

### 10.2 PDF 驱动工厂

**文件**: [PDFService.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Services/PDFService.php)

**工厂类**: `PDFDriverFactory`（第 27-37 行）
```php
class PDFDriverFactory
{
    public static function create(string $driver)
    {
        return match ($driver) {
            'dompdf' => App::make('dompdf.wrapper'),
            'gotenberg' => new GotenbergPDFDriver,
            default => throw new \InvalidArgumentException('Invalid PDFDriver requested')
        };
    }
}
```

### 10.3 DomPDF 驱动

- **包**: `barryvdh/laravel-dompdf`
- **服务容器绑定**: `dompdf.wrapper`
- **特点**: PHP 原生 PDF 生成库，无需外部服务
- **安全配置**: `DOMPDF_ENABLE_REMOTE=false`（默认关闭远程资源加载，防止 SSRF）

### 10.4 Gotenberg 驱动

**实现文件**: [GotenbergPDFDriver.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Services/PDFDrivers/GotenbergPDFDriver.php)

**工作原理**:
1. 先使用 Blade 渲染 HTML 视图：`view($viewname)->render()`
2. 通过 HTTP 调用 Gotenberg 服务（Chromium 引擎）生成 PDF
3. 返回 `GotenbergPDFResponse` 包装响应

**关键参数**:
- **Host**: 从 `config('pdf.connections.gotenberg.host')` 读取，默认 `http://pdf:3000`
- **纸张大小**: 从配置读取，默认 `210mm 297mm`（A4）
- **边距**: 设置为 0，由 CSS 控制

### 10.5 Facade 与服务提供者

**Facade**: [PDF.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Facades/PDF.php)
- 绑定到容器中的 `pdf.driver`

**服务提供者**: [PDFServiceProvider.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Providers/PDFServiceProvider.php)
- 将 `pdf.driver` 绑定到 `PDFService::class`

### 10.6 PDF 生成调用链

以发票为例：

```
Invoice::getPDFData()
        │
        ├─► 准备数据（invoice, company, taxes, 地址格式等）
        ├─► 设置 locale
        ├─► view()->share(...) 共享数据
        ├─► 选择模板路径
        └─► PDF::loadView($templatePath)
                │
                ▼
          PDFService::loadView()
                │
                ▼
          PDFDriverFactory::create($driver)
                │
                ├─► dompdf → DomPDF 实例直接渲染
                └─► gotenberg → Blade 渲染 HTML → HTTP 调用 Gotenberg
                │
                ▼
          返回 PDF 响应对象（含 stream() / output() 方法）
```

### 10.7 邮件附件中的使用

在 `Mailable::build()` 方法中通过 `attachData()` 添加附件：

**示例**: [SendInvoiceMail.php 第 56-61 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/SendInvoiceMail.php#L56-L61)
```php
if ($this->data['attach']['data']) {
    $mailContent->attachData(
        $this->data['attach']['data']->output(),
        $this->data['invoice']['invoice_number'].'.pdf'
    );
}
```

- `$this->data['attach']['data']` 是 PDF 驱动返回的对象
- 调用 `output()` 方法获取 PDF 二进制内容
- 附件命名为 `{invoice_number}.pdf`

---

## 十一、EmailLog Token 反向闭环：从客户点击链接到触发查看通知

### 11.1 闭环全流程概览

```
邮件中的链接 URL（含 token）
        │
        ▼
  Laravel 路由匹配 + 隐式路由模型绑定
        │
        ▼
  Hashids 解码 token → 查找 EmailLog 记录
        │
        ▼
  通过 EmailLog.mailable_id 回查发票/估价单
        │
        ▼
  检查链接是否过期
        │
        ├─► 已过期 → abort(403, 'Link Expired.')
        └─► 未过期 → 更新查看状态 + 触发查看通知邮件
```

### 11.2 第一步：路由定义与 Token 在 URL 中的位置

**路由文件**: [web.php 第 103-112 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/routes/web.php#L103-L112)

```php
Route::prefix('/customer')->group(function () {
    Route::get('/invoices/{email_log:token}', [CustomerInvoicePdfController::class, 'getInvoice']);
    Route::get('/invoices/view/{email_log:token}', [CustomerInvoicePdfController::class, 'getPdf'])->name('invoice');

    Route::get('/estimates/{email_log:token}', [CustomerEstimatePdfController::class, 'getEstimate']);
    Route::get('/estimates/view/{email_log:token}', [CustomerEstimatePdfController::class, 'getPdf'])->name('estimate');

    Route::get('/payments/{email_log:token}', [CustomerPaymentPdfController::class, 'getPayment']);
    Route::get('/payments/view/{email_log:token}', [CustomerPaymentPdfController::class, 'getPdf'])->name('payment');
});
```

**关键语法**: `{email_log:token}` 表示使用 `EmailLog` 模型，以 `token` 字段进行隐式路由模型绑定。

**URL 格式**:
- 发票查看: `GET /customer/invoices/view/{token}`
- 发票数据: `GET /customer/invoices/{token}`
- 估价单查看: `GET /customer/estimates/view/{token}`
- 付款查看: `GET /customer/payments/view/{token}`

### 11.3 第二步：Token 生成（正向链路回顾）

**在 Mailable::build() 中生成**: [SendInvoiceMail.php 第 36-50 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Mail/SendInvoiceMail.php#L36-L50)

```php
$log = EmailLog::create([
    'from' => $this->data['from'],
    'to' => $this->data['to'],
    'cc' => $this->data['cc'] ?? null,
    'bcc' => $this->data['bcc'] ?? null,
    'subject' => $this->data['subject'],
    'body' => $this->data['body'],
    'mailable_type' => Invoice::class,   // 多态类型
    'mailable_id' => $this->data['invoice']['id'],  // 多态 ID
]);

$log->token = Hashids::connection(EmailLog::class)->encode($log->id);
$log->save();

$this->data['url'] = route('invoice', ['email_log' => $log->token]);
```

**Token 生成过程**:
1. 先创建 `EmailLog` 记录获得自增 `id`
2. 使用 `Hashids` 将 `id` 编码为不可猜测的 token
3. 将 token 保存到 `EmailLog.token` 字段
4. 用 token 生成 URL: `/customer/invoices/view/{token}`

**Hashids 配置**: [hashids.php 第 63-67 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/config/hashids.php#L63-L67)

```php
EmailLog::class => [
    'salt' => EmailLog::class.config('app.key'),
    'length' => 20,
    'alphabet' => 'BA5tJUVNPe93fCq6DHlY2x4ZO1Kg7i8wSm0R',
],
```

- **Salt**: 类名 + APP_KEY，确保不同模型的 token 互不碰撞
- **长度**: 20 字符
- **字母表**: 自定义 36 字符集，避免混淆字符

### 11.4 第三步：Laravel 隐式路由模型绑定解析 Token

当客户访问 `GET /customer/invoices/view/BA5tJUVNPe93fCq6DHlY` 时：

1. **Laravel 路由解析**: `{email_log:token}` 告诉框架使用 `EmailLog` 模型的 `token` 字段查询
2. **自动执行**: `EmailLog::where('token', 'BA5tJUVNPe93fCq6DHlY')->first()`
3. **注入控制器**: 查询结果作为 `$emailLog` 参数注入

> 此步骤**不需要** Hashids 解码——因为 `token` 是直接存储在数据库中的 Hashids 编码值，Laravel 的路由模型绑定直接用 `token` 字段做 `WHERE` 查询。

### 11.5 第四步：控制器处理查看请求

**发票控制器**: [InvoicePdfController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Customer/InvoicePdfController.php)

```php
public function getPdf(EmailLog $emailLog, Request $request)
{
    $invoice = Invoice::find($emailLog->mailable_id);  // ① 通过多态 ID 回查发票

    if (! $emailLog->isExpired()) {                      // ② 检查链接是否过期
        if ($invoice && ($invoice->status == Invoice::STATUS_SENT
            || $invoice->status == Invoice::STATUS_DRAFT)) {  // ③ 只在 SENT/DRAFT 状态触发
            $invoice->status = Invoice::STATUS_VIEWED;   // ④ 更新状态为 VIEWED
            $invoice->viewed = true;
            $invoice->save();

            $notifyInvoiceViewed = CompanySetting::getSetting(
                'notify_invoice_viewed', $invoice->company_id
            );                                             // ⑤ 读取公司通知设置

            if ($notifyInvoiceViewed == 'YES') {           // ⑥ 判断是否需要通知
                $data['invoice'] = Invoice::findOrFail($invoice->id)->toArray();
                $data['user'] = Customer::find($invoice->customer_id)->toArray();
                $notificationEmail = CompanySetting::getSetting(
                    'notification_email', $invoice->company_id
                );                                         // ⑦ 读取通知目标邮箱

                \Mail::to($notificationEmail)->send(new InvoiceViewedMail($data));  // ⑧ 发送查看通知
            }
        }

        if ($request->has('pdf')) {                       // ⑨ 直接返回 PDF
            return $invoice->getGeneratedPDFOrStream('invoice');
        }

        return view('app')->with([                         // ⑩ 返回 Vue 应用页面
            'customer_logo' => ...,
            'current_theme' => ...,
        ]);
    }

    abort(403, 'Link Expired.');                          // ⑪ 链接过期
}
```

### 11.6 第五步：链接过期检查

**模型方法**: [EmailLog.php 第 21-33 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/EmailLog.php#L21-L33)

```php
public function isExpired()
{
    $linkExpiryDays = (int) CompanySetting::getSetting(
        'link_expiry_days', $this->mailable()->get()->toArray()[0]['company_id']
    );
    $checkExpiryLinks = CompanySetting::getSetting(
        'automatically_expire_public_links', ...
    );

    $expiryDate = $this->created_at->addDays($linkExpiryDays);

    if ($checkExpiryLinks == 'YES' && Carbon::now()->format('Y-m-d') > $expiryDate->format('Y-m-d')) {
        return true;
    }

    return false;
}
```

**过期判断逻辑**:
1. 从 `CompanySetting` 读取 `link_expiry_days`（链接有效天数）
2. 从 `CompanySetting` 读取 `automatically_expire_public_links`（是否启用自动过期）
3. 仅当开关为 `YES` 且当前日期超过 `created_at + 有效天数` 时返回 `true`

### 11.7 三种实体的查看闭环差异

| 步骤 | 发票 (Invoice) | 估价单 (Estimate) | 付款 (Payment) |
|------|---------------|------------------|----------------|
| 控制器 | `Customer\InvoicePdfController` | `Customer\EstimatePdfController` | `Customer\PaymentPdfController` |
| 回查方式 | `Invoice::find($emailLog->mailable_id)` | `Estimate::find($emailLog->mailable_id)` | `$emailLog->mailable` (多态关联) |
| 状态更新 | `STATUS_VIEWED` + `viewed=true` | `STATUS_VIEWED` | 无状态更新 |
| 查看通知 | `InvoiceViewedMail` | `EstimateViewedMail` | 无查看通知 |
| 非PDF响应 | Vue SPA 页面 | 直接返回 PDF 流 | 直接返回 PDF 流 |
| 通知设置键 | `notify_invoice_viewed` | `notify_estimate_viewed` | 无 |

### 11.8 闭环完整时序图

```
[正向链路] 管理员发送邮件
    │
    ├─ Invoice::send() → 组装数据 → Mail::send(SendInvoiceMail)
    ├─ SendInvoiceMail::build()
    │    ├─ EmailLog::create(...) → 获得 id
    │    ├─ Hashids::encode(id) → 生成 token
    │    ├─ 保存 token 到 EmailLog
    │    └─ route('invoice', ['email_log' => token]) → 生成 URL
    └─ 邮件投递给客户

    ═══════════════ 客户收到邮件 ═══════════════

[反向链路] 客户点击链接
    │
    ├─ GET /customer/invoices/view/{token}
    │
    ├─ Laravel 路由模型绑定: EmailLog::where('token', token)->first()
    │
    ├─ Invoice::find($emailLog->mailable_id) → 回查发票
    │
    ├─ $emailLog->isExpired() → 检查链接是否过期
    │   └─ 读取 CompanySetting: link_expiry_days + automatically_expire_public_links
    │
    ├─ [未过期 & 状态为 SENT/DRAFT]
    │   ├─ $invoice->status = VIEWED, $invoice->viewed = true → 保存
    │   ├─ CompanySetting::getSetting('notify_invoice_viewed')
    │   ├─ [设置为 YES]
    │   │   ├─ 读取 notification_email
    │   │   └─ Mail::to(notificationEmail)->send(new InvoiceViewedMail) → 通知管理员
    │   └─ 返回 Vue SPA / PDF 流
    │
    └─ [已过期] → abort(403, 'Link Expired.')
```

---

## 十二、Mail Driver 投递通道选择与动态配置热加载

### 12.1 Laravel Mail 投递通道架构

Laravel 邮件投递通道由 `mail.default` 配置决定，对应 `config/mail.php` 中的 `mailers` 数组：

```
config('mail.default')
        │
        ▼
  Mailer 实例选择对应的 Transport
        │
        ├─► smtp    → SmtpTransport (SMTP 协议直连)
        ├─► mailgun → MailgunTransport (Mailgun HTTP API)
        ├─► ses     → SesTransport (AWS SES API)
        ├─► sendmail → SendmailTransport (本地 sendmail 命令)
        ├─► log     → LogTransport (写入日志，不实际发送)
        └─► array   → ArrayTransport (内存数组，测试用)
```

### 12.2 配置双层存储机制

InvoiceShelf 的邮件配置有**两个存储层**：

#### 第一层：.env 文件（静态/安装时）
- 通过 `env()` 函数读取
- 仅在安装向导中使用 [EnvironmentManager](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Space/EnvironmentManager.php) 写入
- 修改后需重启服务才能生效

#### 第二层：settings 数据库表（动态/运行时）
- 通过 [Setting::setSettings()](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Models/Setting.php#L31-L44) 写入
- 每次请求启动时由 `AppConfigProvider` 读取并覆盖到 `Config` facade
- **无需重启即可生效**

### 12.3 运行时热加载实现

**核心 Provider**: [AppConfigProvider.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Providers/AppConfigProvider.php)

**注册顺序**: [providers.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/bootstrap/providers.php#L18-L18) 中 `AppConfigProvider` 排在**最后**，确保覆盖其他 Provider 的配置

```php
return [
    HashidsServiceProvider::class,
    AppServiceProvider::class,
    RouteServiceProvider::class,
    DropboxServiceProvider::class,
    ViewServiceProvider::class,
    PDFServiceProvider::class,
    AppConfigProvider::class,   // ← 最后加载，覆盖上方配置
];
```

**boot() 方法执行时机**: 每个请求启动时

```php
public function boot(): void
{
    if (! InstallUtils::isDbCreated()) {
        return;  // 安装前跳过
    }

    $this->configureMailFromDatabase();   // 热加载邮件配置
    $this->configurePDFFromDatabase();    // 热加载 PDF 配置
    $this->configureFileSystemFromDatabase();  // 热加载文件系统配置
}
```

### 12.4 configureMailFromDatabase() 热加载细节

[AppConfigProvider.php 第 31-113 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Providers/AppConfigProvider.php#L31-L113)

**执行步骤**:

1. **从数据库读取邮件设置**:
```php
$mailSettings = Setting::getSettings([
    'mail_driver', 'mail_host', 'mail_port', 'mail_username',
    'mail_password', 'mail_encryption', 'mail_scheme', 'mail_url',
    'mail_timeout', 'mail_local_domain', 'from_name', 'from_mail',
    'mail_mailgun_domain', 'mail_mailgun_secret', 'mail_mailgun_endpoint',
    'mail_mailgun_scheme', 'mail_ses_key', 'mail_ses_secret',
    'mail_ses_region', 'mail_sendmail_path',
]);
```

2. **设置默认 mailer**:
```php
Config::set('mail.default', $driver);
```

3. **根据驱动类型写入对应配置**:

| 驱动 | 写入的配置项 |
|-----|------------|
| `smtp` | `mail.mailers.smtp.host/port/username/password/encryption/scheme/url/timeout/local_domain` |
| `mailgun` | `mail.mailers.mailgun.domain/secret/endpoint/scheme` + `services.mailgun.*` |
| `ses` | `services.ses.key/secret/region` |
| `sendmail` | `mail.mailers.sendmail.path` |

4. **设置全局 from 地址**:
```php
Config::set('mail.from.address', $mailSettings['from_mail']);
Config::set('mail.from.name', $mailSettings['from_name']);
```

### 12.5 管理员切换配置的完整流程

#### 保存配置 API

**控制器**: [MailConfigurationController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Http/Controllers/V1/Admin/Settings/MailConfigurationController.php)

`saveMailEnvironment()` 方法（第 40-59 行）:

```
PUT /api/v1/admin/settings/mail
  → 授权检查: 'manage email config'
  → prepareMailSettingsForDatabase($request)
     → 根据驱动类型组装设置数组
  → Setting::setSettings($mailSettings)
     → 逐项 updateOrCreate 到 settings 表
```

#### 热加载生效路径

```
管理员保存邮件配置
        │
        ▼
  Setting::setSettings() → 写入 settings 数据库表
        │
        ▼
  下一次 HTTP 请求启动
        │
        ▼
  AppConfigProvider::boot()
        │
        ▼
  configureMailFromDatabase()
        │
        ├─► Setting::getSettings() → 从数据库读取最新配置
        ├─► Config::set('mail.default', $driver) → 切换默认 mailer
        ├─► Config::set('mail.mailers.smtp.*', ...) → 更新 SMTP 参数
        ├─► Config::set('services.mailgun.*', ...) → 更新 Mailgun 参数
        ├─► Config::set('services.ses.*', ...) → 更新 SES 参数
        └─► Config::set('mail.from.*', ...) → 更新发件人信息
        │
        ▼
  后续 Mail::to()->send() 使用新配置投递
```

### 12.6 关键技术细节

#### Config::set() 的运行时覆盖机制
- `Config::set()` 修改的是**当前请求进程**内存中的配置值
- Laravel 的 `MailManager` 在每次发送邮件时，通过 `Config::get('mail.default')` 获取当前 mailer
- 因此 `AppConfigProvider::boot()` 在每个请求开始时将数据库配置写入内存，后续邮件发送即使用新配置

#### 为什么不直接写 .env
- `.env` 文件修改需要重启 PHP 进程（FPM/Octane）才能生效
- 数据库 + `Config::set()` 方案实现了**无需重启**的配置切换
- 这是 InvoiceShelf 作为自托管 SaaS 的核心设计：管理员在 UI 修改配置后立即生效

#### 降级与异常处理
- `configureMailFromDatabase()` 被包裹在 `try-catch` 中（第 109-112 行）
- 数据库不可用时（安装阶段/迁移中）静默跳过
- 此时使用 `config/mail.php` 中的 `.env` 默认值作为兜底

#### Demo 模式邮件拦截
[AppServiceProvider.php 第 70-73 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Providers/AppServiceProvider.php#L70-L73)

```php
if (config('app.env') === 'demo') {
    Mail::fake();
    Notification::fake();
}
```

在 `demo` 环境下，`Mail::fake()` 会拦截所有邮件发送，转而记录到内存中（用于断言），不会实际投递。

### 12.7 PDF 配置的热加载

与邮件配置同构，[AppConfigProvider.php 第 118-158 行](file:///d:/fz/0601-1/solo-dogfeeding/code/68-InvoiceShelf/app/Providers/AppConfigProvider.php#L118-L158)：

```php
protected function configurePDFFromDatabase(): void
{
    $pdfSettings = Setting::getSettings([
        'pdf_driver', 'gotenberg_host', 'gotenberg_papersize', 'gotenberg_margins',
    ]);

    if (! empty($pdfSettings['pdf_driver'])) {
        Config::set('pdf.driver', $pdfSettings['pdf_driver']);
        // + Gotenberg 连接参数
    }
}
```

管理员在设置页面切换 PDF 驱动后，下次请求 `PDFService::loadView()` 会读取更新后的 `config('pdf.driver')`，选择对应的驱动实例。

### 12.8 配置热加载的时序图

```
┌──────────────────────────────────────────────────────────┐
│                    管理员操作                              │
│                                                          │
│  1. PUT /api/v1/admin/settings/mail                      │
│     → Setting::setSettings() → 写入 settings 表          │
│     → 返回 JSON 成功                                      │
│                                                          │
│  2. 后续任意 HTTP 请求                                     │
│     → Laravel 启动 → Providers boot()                    │
│     → AppConfigProvider::boot()                          │
│     → configureMailFromDatabase()                        │
│     → Setting::getSettings() 从数据库读取                  │
│     → Config::set('mail.default', 'smtp')                │
│     → Config::set('mail.mailers.smtp.host', '...')       │
│     → ...                                                │
│                                                          │
│  3. 邮件发送操作                                          │
│     → Mail::to()->send()                                 │
│     → MailManager 读取 config('mail.default') = 'smtp'   │
│     → 创建 SmtpTransport 使用 config('mail.mailers.smtp')│
│     → 投递成功                                            │
└──────────────────────────────────────────────────────────┘
```
