# 客户门户（Customer Portal）会话建立与权限边界分析

## 一、架构概览

InvoiceShelf 采用**双 Guard 隔离架构**，将管理后台（Admin）与客户门户（Customer Portal）的认证体系完全分离。Admin 使用 `web` / `sanctum` Guard，Customer Portal 使用独立的 `customer` Guard，二者共享同一 Laravel 应用但通过不同的认证守卫、中间件和路由前缀实现权限隔离。

```
┌──────────────────────────────────────────────────────────┐
│                    Laravel Application                    │
│                                                          │
│  ┌─────────────────────┐    ┌─────────────────────────┐  │
│  │   Admin Guard (web)  │    │  Customer Guard (customer)│ │
│  │   Provider: users    │    │  Provider: customers      │ │
│  │   Model: User        │    │  Model: Customer          │ │
│  │   Driver: session    │    │  Driver: session          │ │
│  └─────────┬───────────┘    └───────────┬──────────────┘  │
│            │                            │                 │
│   auth:sanctum + company + bouncer   auth:customer        │
│            │                      + customer-portal       │
│            │                            │                 │
│  ┌─────────▼───────────┐    ┌───────────▼──────────────┐  │
│  │   Admin Routes       │    │  Customer Portal Routes   │ │
│  │   /api/v1/*          │    │  /api/v1/{company:slug}   │ │
│  │                      │    │  /customer/*              │ │
│  └──────────────────────┘    └───────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

## 二、认证配置层

### 2.1 Guard 定义

文件：[auth.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/config/auth.php#L41-L56)

```php
'guards' => [
    'web' => [
        'driver' => 'session',
        'provider' => 'users',
    ],
    'api' => [
        'driver' => 'token',
        'provider' => 'users',
        'hash' => false,
    ],
    'customer' => [
        'driver' => 'session',
        'provider' => 'customers',
        'hash' => false,
    ],
],
```

关键设计：

- `customer` Guard 使用 `session` 驱动，意味着客户门户通过 Cookie/Session 机制维持会话状态
- `customer` Guard 绑定到 `customers` Provider，该 Provider 使用 `Customer` 模型
- 这确保了 Admin 的 `web` Guard 和 Customer 的 `customer` Guard 的 Session 数据互不干扰

### 2.2 Provider 定义

文件：[auth.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/config/auth.php#L58-L67)

```php
'providers' => [
    'users' => [
        'driver' => 'eloquent',
        'model' => User::class,
    ],
    'customers' => [
        'driver' => 'eloquent',
        'model' => Customer::class,
    ],
],
```

### 2.3 密码重置 Broker

文件：[auth.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/config/auth.php#L88-L101)

```php
'passwords' => [
    'users' => [
        'provider' => 'users',
        'table' => 'password_reset_tokens',
        'expire' => 60,
        'throttle' => 60,
    ],
    'customers' => [
        'provider' => 'customers',
        'table' => 'password_reset_tokens',
        'expire' => 60,
        'throttle' => 60,
    ],
],
```

客户使用独立的 `customers` 密码重置 Broker，与 Admin 的 `users` Broker 分离，保证密码重置令牌不会跨身份体系混用。

## 三、会话建立流程

### 3.1 登录入口

客户门户的登录有两个路由入口：

**Web 路由（SPA 入口）：**

文件：[web.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L45)

```php
Route::post('/{company:slug}/customer/login', CustomerLoginController::class);
```

**API 路由（认证子组）：**

文件：[api.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/api.php#L494-L501)

```php
Route::prefix('/{company:slug}/customer')->group(function () {
    Route::prefix('auth')->group(function () {
        Route::post('password/email', [AuthForgotPasswordController::class, 'sendResetLinkEmail']);
        Route::post('reset/password', [AuthResetPasswordController::class, 'reset'])->name('customer.password.reset');
    });
    // ...
});
```

注意：登录路由（`POST /{company:slug}/customer/login`）位于 `web.php` 中，而密码重置路由位于 `api.php` 中。

### 3.2 登录控制器

文件：[LoginController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/LoginController.php#L21-L44)

```php
public function __invoke(CustomerLoginRequest $request, Company $company)
{
    $user = Customer::where('email', $request->email)
        ->where('company_id', $company->id)
        ->first();

    if (! $user || ! Hash::check($request->password, $user->password)) {
        throw ValidationException::withMessages([
            'email' => ['The provided credentials are incorrect.'],
        ]);
    }

    if (! $user->enable_portal) {
        throw ValidationException::withMessages([
            'email' => ['Customer portal not available for this user.'],
        ]);
    }

    Auth::guard('customer')->login($user);

    return response()->json([
        'success' => true,
    ]);
}
```

登录流程的三重校验：

| 步骤 | 校验内容 | 失败行为 |
|------|----------|----------|
| 1 | 通过 `email` + `company_id` 查找客户记录 | 抛出 `ValidationException`（凭据错误） |
| 2 | 密码哈希比对 `Hash::check()` | 抛出 `ValidationException`（凭据错误） |
| 3 | `enable_portal` 布尔字段检查 | 抛出 `ValidationException`（门户不可用） |

关键细节：查询时同时限定了 `company_id`，即**客户必须在指定公司下**才能登录，这构成了公司级别的租户隔离。

### 3.3 登出

文件：[web.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L47-L49)

```php
Route::post('/{company:slug}/customer/logout', function () {
    Auth::guard('customer')->logout();
});
```

直接调用 `customer` Guard 的 `logout()` 方法清除 Session。

### 3.4 密码重置

- **忘记密码**：[ForgotPasswordController](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/ForgotPasswordController.php) 使用 `Password::broker('customers')` 发送重置邮件
- **重置密码**：[ResetPasswordController](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/ResetPasswordController.php) 同样使用 `customers` Broker，重置成功后重定向到 `RouteServiceProvider::CUSTOMER_HOME`（`/customer/dashboard`）

## 四、权限边界实现机制

### 4.1 `enable_portal` 字段 — 门户访问的总开关

文件：[Customer.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Models/Customer.php#L46-L51)

```php
protected function casts(): array
{
    return [
        'enable_portal' => 'boolean',
    ];
}
```

数据库迁移（[change_enable_portal_field](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/database/migrations/2021_12_21_102521_change_enable_portal_field_of_customers_table.php#L15-L17)）：

```php
Schema::table('customers', function (Blueprint $table) {
    $table->boolean('enable_portal')->default(false)->change();
});
```

`enable_portal` 默认为 `false`，即新创建的客户默认**没有**门户访问权限，必须由管理员显式启用。这是一个 "deny by default" 的安全策略。

### 4.2 双重 `enable_portal` 校验

`enable_portal` 在两个位置被检查，形成**双重防线**：

#### 第一层：登录时校验

文件：[LoginController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/LoginController.php#L33-L37)

```php
if (! $user->enable_portal) {
    throw ValidationException::withMessages([
        'email' => ['Customer portal not available for this user.'],
    ]);
}
```

在凭证验证通过后、Session 建立前，检查 `enable_portal`。如果为 `false`，拒绝登录。

#### 第二层：中间件持续校验

文件：[CustomerPortalMiddleware.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/CustomerPortalMiddleware.php#L19-L30)

```php
public function handle(Request $request, Closure $next): Response
{
    $user = Auth::guard('customer')->user();

    if (! $user->enable_portal) {
        Auth::guard('customer')->logout();

        return response('Unauthorized.', 401);
    }

    return $next($request);
}
```

在**每次请求**时检查已认证客户的 `enable_portal` 状态。如果管理员在客户登录后禁用了门户权限，该客户在下次请求时会被**强制登出**并返回 401。

这种双重校验的设计确保了：
- 未授权客户无法建立会话（第一层）
- 已登录客户的权限变更能即时生效（第二层）

### 4.3 中间件注册与路由绑定

#### 中间件别名注册

文件：[app.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/bootstrap/app.php#L70-L84)

```php
$middleware->alias([
    'auth' => Authenticate::class,
    'customer' => CustomerRedirectIfAuthenticated::class,
    'customer-guest' => CustomerGuest::class,
    'customer-portal' => CustomerPortalMiddleware::class,
    // ...
]);
```

与客户门户相关的中间件别名有三个：

| 别名 | 中间件类 | 作用 |
|------|----------|------|
| `customer` | `CustomerRedirectIfAuthenticated` | 已认证客户访问 guest 路由时重定向 |
| `customer-guest` | `CustomerGuest` | 委托给 `CustomerRedirectIfAuthenticated` |
| `customer-portal` | `CustomerPortalMiddleware` | 校验 `enable_portal` 权限 |

#### 路由中间件组

文件：[api.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/api.php#L506)

```php
Route::middleware(['auth:customer', 'customer-portal'])->group(function () {
    // 所有客户门户受保护的路由
});
```

客户门户的受保护路由使用**中间件栈** `auth:customer` + `customer-portal`：
1. `auth:customer`：确保请求已通过 `customer` Guard 认证（未认证返回 401）
2. `customer-portal`：在认证通过后，再次校验 `enable_portal` 权限

### 4.4 公司级租户隔离

文件：[LoginController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/LoginController.php#L23-L25)

```php
$user = Customer::where('email', $request->email)
    ->where('company_id', $company->id)
    ->first();
```

登录查询同时限定 `company_id`，路由参数 `{company:slug}` 通过 Laravel 的 Route Model Binding 自动解析为 `Company` 模型实例。这确保了：

- 客户只能在其所属公司的门户下登录
- 即使不同公司存在相同 email 的客户，也不会跨公司认证

### 4.5 Guard 隔离

Admin 和 Customer Portal 使用完全不同的认证 Guard：

| 维度 | Admin | Customer Portal |
|------|-------|-----------------|
| Guard 名称 | `web` / `sanctum` | `customer` |
| 用户模型 | `User` | `Customer` |
| Session 存储 | 独立 Session | 独立 Session |
| 登录入口 | `POST /login` | `POST /{company:slug}/customer/login` |
| 权限系统 | Bouncer (角色+能力) | `enable_portal` 布尔开关 |

这意味着：
- Admin 的 Session 不会赋予客户门户的访问权限
- Customer 的 Session 不会赋予管理后台的访问权限
- 两者可以同时在线而互不影响

### 4.6 与 Admin 权限系统的对比

Admin 端使用了 Silber/Bouncer 的角色与能力系统（RBAC），通过 [ScopeBouncer](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/ScopeBouncer.php) 中间件按公司租户划定作用域：

```php
$this->bouncer->scope()->to($tenantId);
```

而 Customer Portal 不使用 Bouncer，而是采用更简单的 `enable_portal` 布尔开关作为权限边界。这符合业务逻辑：客户不需要细粒度的角色权限，只需要一个"能否访问门户"的开关。

### 4.7 PDF 访问的特殊权限

文件：[PdfMiddleware.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/PdfMiddleware.php#L19-L26)

```php
if (Auth::guard('web')->check() || Auth::guard('sanctum')->check() || Auth::guard('customer')->check()) {
    return $next($request);
}
```

PDF 中间件接受三种 Guard 的任一认证，即 Admin 用户和已认证客户都能访问 PDF 资源。但客户门户的 PDF 端点还额外支持通过 `email_log:token` 无认证访问（用于邮件中的 PDF 链接）。

## 五、前端 CSRF 防护机制

客户门户是基于 Laravel + Vue 的 SPA 应用，采用了 Laravel 标准的 **Cookie-based CSRF 防护**方案，通过 `XSRF-TOKEN` Cookie 与 `X-XSRF-TOKEN` 请求头的配对验证来防御跨站请求伪造攻击。

### 5.1 后端 CSRF 中间件配置

文件：[PreventRequestForgery.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/PreventRequestForgery.php#L7-L23)

```php
class PreventRequestForgery extends Middleware
{
    protected $addHttpCookie = true;

    protected $except = [
        'login',
    ];
}
```

关键配置：

| 配置项 | 值 | 含义 |
|--------|----|------|
| `$addHttpCookie` | `true` | 响应时自动添加 `XSRF-TOKEN` Cookie，供前端 JS 读取 |
| `$except` | `['login']` | 登录接口豁免 CSRF 校验（因为登录前还没有有效 Token） |

`$addHttpCookie = true` 是前端能够读取 CSRF Token 的前提——Laravel 会在响应中设置一个名为 `XSRF-TOKEN` 的 Cookie，前端 JavaScript 可以读取该 Cookie 并在后续请求中以 `X-XSRF-TOKEN` 头发送回去。

### 5.2 前端 Axios 配置

文件：[http/index.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/http/index.js#L1-L28)

```js
const instance = axios.create({
  withCredentials: true,
  headers: {
    common: {
      'X-Requested-With': 'XMLHttpRequest',
    },
  },
})
```

CSRF 防护的前端配置要点：

1. **`withCredentials: true`**：允许 Axios 在跨域请求中携带 Cookie（包括 Session Cookie 和 XSRF-TOKEN Cookie）。这是 Laravel SPA 认证模式的核心配置。
2. **`X-Requested-With: XMLHttpRequest`**：标识请求为 AJAX 请求，Laravel 会据此判断是否以 AJAX 方式响应（如返回 JSON 而非重定向）。

> **注意**：代码中的 Axios 实例并未显式设置 `X-XSRF-TOKEN` 请求头。这是因为 Axios 默认会自动从 `XSRF-TOKEN` Cookie 中读取值并添加到 `X-XSRF-TOKEN` 请求头中——只要 `withCredentials` 为 `true` 且 Cookie 存在，Axios 会自动处理。

### 5.3 CSRF Token 获取流程

客户门户登录前需要先获取 CSRF Cookie：

文件：[auth.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L19-L43)

```js
login(data) {
  return new Promise((resolve, reject) => {
    http.get('/sanctum/csrf-cookie').then((response) => {
      if (response) {
        http
          .post(`/${data.company}/customer/login`, data)
          .then(...)
      }
    })
  })
}
```

完整的 CSRF 初始化流程：

```
┌──────────────────────────────────────────────────────────┐
│  1. 前端调用 GET /sanctum/csrf-cookie                     │
│     （触发 Laravel 设置 XSRF-TOKEN Cookie 和 Session Cookie）│
└──────────────────────┬───────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  2. 浏览器存储两个 Cookie：                                │
│     • 应用名_session（Session ID，HttpOnly）              │
│     • XSRF-TOKEN（CSRF Token，可被 JS 读取）              │
└──────────────────────┬───────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  3. 前端发送 POST /{company}/customer/login               │
│     Axios 自动：                                          │
│     • 携带 Cookie（withCredentials: true）               │
│     • 从 XSRF-TOKEN Cookie 读取值并添加 X-XSRF-TOKEN 头  │
└──────────────────────┬───────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  4. 后端 PreventRequestForgery 中间件校验：               │
│     比较请求头中的 X-XSRF-TOKEN 与会话中的 CSRF Token     │
│     匹配 → 通过；不匹配 → 419 Page Expired               │
└──────────────────────────────────────────────────────────┘
```

### 5.4 深度审计：CSRF 豁免路径的真实情况

此前文档将 `$except = ['login']` 描述为"登录接口豁免 CSRF 校验"，但经过深度代码审计，实际情况更为复杂：

#### Laravel CSRF `$except` 的匹配机制

Laravel 框架的 `VerifyCsrfToken` 中间件（[PreventRequestForgery](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/PreventRequestForgery.php#L21-L23) 继承自它）使用 `$request->is()` 方法对 `$except` 数组中的路径进行匹配。`is()` 方法是前缀匹配，支持通配符 `*`。

配置值 `'login'` 只能匹配路径为 `/login` 的请求——即 Admin 登录（[web.php:33](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L33)）。

而客户登录路径是 `/{company:slug}/customer/login`（[web.php:45](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L45)），**并不会匹配 `'login'`，因此客户登录请求实际上仍然需要通过 CSRF 校验**。

#### 客户登录为何仍然能正常工作

虽然客户登录不在 `$except` 列表中，但前端登录流程中的第一步就解决了这个问题：

文件：[auth.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L22-L41)

```js
http.get('/sanctum/csrf-cookie').then((response) => {
  if (response) {
    http.post(`/${data.company}/customer/login`, data)
  }
})
```

前端在提交登录表单之前，先调用 `GET /sanctum/csrf-cookie`——这是 Laravel Sanctum 的标准端点，用于初始化会话并下发 `XSRF-TOKEN` Cookie。由于 `GET` 请求本身不经过 CSRF 校验（Laravel 只校验 `POST/PUT/PATCH/DELETE` 等"写操作"），该请求能够成功返回并设置 CSRF Cookie，使得后续的登录 `POST` 请求能够携带有效的 `X-XSRF-TOKEN` 请求头。

#### 真正免于认证校验的完整路径清单

以下是客户门户中**不需要 `auth:customer` + `customer-portal` 中间件**即可访问的路径：

| 路径 | 文件 | HTTP 方法 | 安全说明 |
|------|------|:---------:|----------|
| `/{company:slug}/customer/login` | [web.php:45](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L45) | POST | 登录入口，有凭据校验 + enable_portal 检查 |
| `/{company:slug}/customer/logout` | [web.php:47-49](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L47-L49) | POST | 登出，无会话时调用无副作用 |
| `/api/v1/{company:slug}/customer/auth/password/email` | [api.php:497](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/api.php#L497) | POST | 发送密码重置邮件，有节流保护 |
| `/api/v1/{company:slug}/customer/auth/reset/password` | [api.php:500](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/api.php#L500) | POST | 密码重置，依赖有效 token |
| `/customer/invoices/{email_log:token}` 系列 | [web.php:103-112](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L103-L112) | GET | 邮件 PDF 链接，通过 token 校验替代会话认证 |
| `/sanctum/csrf-cookie` | Sanctum 内置 | GET | CSRF Cookie 初始化，无敏感信息 |
| `/api/v1/ping` | [api.php:129-133](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/api.php#L129-L133) | GET | 健康检查，无敏感信息 |
| `/api/v1/countries` | [api.php:162](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/api.php#L162) | GET | 公开国家列表 |
| `/{company:slug}/customer/{vue?}` | [web.php:129-135](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L129-L135) | GET | SPA 页面入口，仅返回 HTML |

其余所有客户门户的业务 API 均位于 `Route::middleware(['auth:customer', 'customer-portal'])` 分组内（[api.php:506](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/api.php#L506)）。

### 5.5 Blade 模板中的备用 CSRF

文件：[app.blade.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/views/app.blade.php#L17)

```html
<meta name="csrf-token" content="{{ csrf_token() }}">
```

Blade 模板中还以 `<meta>` 标签的形式注入了 CSRF Token，作为传统表单提交或非 Axios 请求时的备用方案。

## 六、Bootstrap 初始化流程

客户门户的前端应用在进入用户态（已登录视图）之前，会调用 Bootstrap 接口获取当前客户的完整上下文信息，完成应用初始化。

### 6.1 前端初始化入口

文件：[LayoutBasic.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/layouts/LayoutBasic.vue#L17-L35)

```vue
<script setup>
const globalStore = useGlobalStore()
const route = useRoute()
const isAppLoaded = computed(() => globalStore.isAppLoaded)

loadData()

async function loadData() {
  await globalStore.bootstrap(route.params.company)
}
</script>
```

`LayoutBasic` 是客户门户所有需要认证的页面的根布局组件。它在组件挂载时立即调用 `bootstrap()`，只有当 `isAppLoaded` 为 `true` 时才渲染实际内容，否则页面处于空白状态。

> **注意**：前端路由的 `meta.requiresAuth` 标记在全局 `router.beforeEach` 中并未实际校验客户的认证状态（只处理了 Admin 端的 ability 检查）。客户门户的认证检查实际上是通过 Bootstrap API 请求来隐式完成的——如果未登录，后端会返回 401，前端错误处理器会自动触发登出并跳转登录页。

### 6.2 Bootstrap Store 实现

文件：[global.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/global.js#L20-L46)

```js
bootstrap(data) {
  this.companySlug = data
  const userStore = useUserStore()
  return new Promise((resolve, reject) => {
    http
      .get(`/api/v1/${data}/customer/bootstrap`)
      .then((response) => {
        this.currentUser = response.data.data
        this.mainMenu = response.data.meta.menu
        this.currency = response.data.data.currency
        this.enabledModules = response.data.meta.modules
        Object.assign(userStore.userForm, response.data.data)
        global.locale.value = response.data.meta.current_company_language || 'en'
        this.isAppLoaded = true
        resolve(response)
      })
  })
}
```

Bootstrap 接口返回的数据会填充到多个 Store 中：

| 数据项 | 存储位置 | 用途 |
|--------|----------|------|
| `data`（客户信息） | `globalStore.currentUser` + `userStore.userForm` | 当前登录客户的完整资料 |
| `meta.menu` | `globalStore.mainMenu` | 客户门户侧边栏/导航菜单 |
| `data.currency` | `globalStore.currency` | 客户默认货币 |
| `meta.modules` | `globalStore.enabledModules` | 已启用的模块列表 |
| `meta.current_company_language` | `i18n.global.locale` | 公司语言设置 |

### 6.3 后端 Bootstrap 接口

文件：[BootstrapController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/General/BootstrapController.php#L21-L41)

```php
public function __invoke(Request $request)
{
    $customer = Auth::guard('customer')->user();

    foreach (\Menu::get('customer_portal_menu')->items->toArray() as $data) {
        $menu[] = [
            'title' => $data->title,
            'link' => $data->link->path['url'],
        ];
    }

    return (new CustomerResource($customer))
        ->additional(['meta' => [
            'menu' => $menu,
            'current_customer_currency' => Currency::find($customer->currency_id),
            'modules' => Module::where('enabled', true)->pluck('name'),
            'current_company_language' => CompanySetting::getSetting('language', $customer->company_id),
        ]]);
}
```

Bootstrap 接口的职责：

1. 通过 `Auth::guard('customer')->user()` 获取当前认证客户
2. 从 `lavary/laravel-menu` 获取 `customer_portal_menu` 菜单配置
3. 查询客户货币、启用模块、公司语言等全局配置
4. 以 `CustomerResource` 格式返回，并附加 `meta` 元数据

该接口受 `auth:customer` + `customer-portal` 双重中间件保护，确保只有已登录且启用了门户权限的客户才能调用。

### 6.4 登录态失效后的前端处理链路（深度审计）

客户门户没有前端路由守卫（`router.beforeEach` 中仅处理 Admin 端的 ability 检查），因此依赖后端 401 响应来被动触发登出跳转。本节逐行追踪代码，完整还原 401 返回后前端的实际执行路径。

#### 6.4.1 401 触发源：后端返回的两种 401 响应

客户门户的 401 响应有两种产生路径：

**路径 A：`auth:customer` 中间件（Session 过期或无效）**

文件：[Authenticate.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/Authenticate.php#L16-L21)

```php
protected function redirectTo($request)
{
    if (! $request->expectsJson()) {
        return route('login');
    }
}
```

当请求携带 `X-Requested-With: XMLHttpRequest` 头（前端 Axios 默认添加）时，`expectsJson()` 返回 `true`，Laravel 不做重定向而是直接返回 **HTTP 401**，响应体为空。

**路径 B：`customer-portal` 中间件（enable_portal 被禁用）**

文件：[CustomerPortalMiddleware.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/CustomerPortalMiddleware.php#L19-L30)

```php
public function handle(Request $request, Closure $next): Response
{
    $user = Auth::guard('customer')->user();

    if (! $user->enable_portal) {
        Auth::guard('customer')->logout();

        return response('Unauthorized.', 401);
    }

    return $next($request);
}
```

此路径返回 **HTTP 401**，响应体为字符串 `'Unauthorized.'`。

两种 401 响应的对比：

| 产生路径 | HTTP 状态码 | `response.data` | `response.statusText` |
|----------|:-----------:|------------------|-----------------------|
| 路径 A（Session 过期） | 401 | 空（或 `{}`） | `'Unauthorized'` |
| 路径 B（门户被禁用） | 401 | `'Unauthorized.'`（字符串） | `'Unauthorized'` |

#### 6.4.2 前端全局错误处理器

文件：[error-handling.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/helpers/error-handling.js#L4-L40)

```js
export const handleError = (err) => {
  const authStore = useAuthStore()
  const notificationStore = useNotificationStore()

  if (!err.response) {
    notificationStore.showNotification({ type: 'error', message: '...' })
  } else {
    if (
      err.response.data &&
      (err.response.statusText === 'Unauthorized' ||
        err.response.data === ' Unauthorized.')
    ) {
      const msg = err.response.data.message
        ? err.response.data.message
        : 'Unauthorized'

      showToaster(msg)

      authStore.logout()
    } else if (err.response.data.errors) {
      // ...
    } else if (err.response.data.error) {
      showError(err.response.data.error)
    } else {
      showError(err.response.data.message)
    }
  }
}
```

**401 识别条件的逐项分析**：

条件组合为 `err.response.data && (A || B)`，其中：

- **条件 A**：`err.response.statusText === 'Unauthorized'`

  Axios 的 `statusText` 取自 HTTP 响应行。根据 HTTP/1.1 规范（RFC 7231 6.3.1），401 的 Reason-Phrase 是 `Unauthorized`。绝大多数 Web 服务器（Nginx、Apache）和 Laravel 的默认响应都遵循此规范。因此**条件 A 在绝大多数场景下为 `true`**。

  唯一例外：如果应用部署在自定义 Reason-Phrase 的反向代理之后（极罕见），`statusText` 可能不是 `'Unauthorized'`。

- **条件 B**：`err.response.data === ' Unauthorized.'`

  注意字符串首字符是**空格**。后端 `CustomerPortalMiddleware` 返回的响应体是 `response('Unauthorized.', 401)`，即 `'Unauthorized.'`（无前导空格）。**条件 B 永远为 `false`**——这是一个 Bug，但由于条件 A 通常能覆盖，该 Bug 在运行时影响有限。

- **前置条件**：`err.response.data &&`

  路径 A 返回的 401 响应体可能为空字符串或空对象。空字符串 `''` 在 JavaScript 中为 falsy，此时前置条件失败，**整个 401 分支被跳过**。这意味着 Session 过期（路径 A）导致的 401 可能无法被正确识别——如果 `response.data` 恰好是空字符串的话。

**识别条件稳定性评估**：

| 401 场景 | `statusText` 判断 | `data` 字符串判断 | 整体是否能命中 |
|----------|:-----------------:|:-----------------:|:-------------:|
| Session 过期（路径 A） | ✅ 通常有效 | ❌ data 可能为空 | ⚠️ 取决于 `statusText` |
| 门户被禁用（路径 B） | ✅ 通常有效 | ❌ 前导空格 Bug | ✅ 靠 `statusText` 兜底 |

结论：当前识别条件**不够稳定**。条件 B 是一个死代码（前导空格 Bug），前置条件 `err.response.data &&` 在路径 A 场景下可能失败。更可靠的做法是直接判断 `err.response.status === 401`。

#### 6.4.3 `authStore.logout()` 调用缺少公司参数

当 401 识别条件命中后，代码调用 `authStore.logout()`——**没有传入任何参数**。

文件：[error-handling.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/helpers/error-handling.js#L27)

```js
authStore.logout()
```

对比正常登出时的调用方式。用户点击登出按钮时：

文件：[TheSiteHeader.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/layouts/partials/TheSiteHeader.vue#L259-L265)

```js
function logout() {
  authStore.logout(globalStore.companySlug).then((res) => {
    if (res) {
      router.push({ name: 'customer.login' })
    }
  })
}
```

正常登出传入了 `globalStore.companySlug` 作为公司标识，而错误处理器中未传入此参数。

`logout` 方法的实现：

文件：[auth.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L102-L120)

```js
logout(data) {
  return new Promise((resolve, reject) => {
    http
      .post(`/${data}/customer/logout`)
      .then((response) => {
        const notificationStore = useNotificationStore()
        notificationStore.showNotification({
          type: 'success',
          message: global.tm('general.logged_out_successfully'),
        })
        router.push({ name: 'customer.login' })
        resolve(response)
      })
      .catch((err) => {
        handleError(err)
        reject(err)
      })
  })
}
```

当 `data` 为 `undefined` 时，模板字符串 `` `/${data}/customer/logout` `` 的求值结果为 `'/undefined/customer/logout'`。

该请求发至后端后的匹配过程：

1. 先匹配 [web.php:47](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L47-L49) 的 `POST /{company:slug}/customer/logout`——理论上 `{company:slug}` 可以匹配 `undefined`，但 `Company::where('slug', 'undefined')` 查询不到记录时，Laravel 抛出 404
2. 如果未被上述路由匹配，则匹配 [web.php:129](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L129-L135) 的 `GET /{company:slug}/customer/{vue?}`——但该路由仅支持 `GET` 方法，`POST` 请求不匹配
3. 最终返回 **404 Not Found** 或 **405 Method Not Allowed**

**结论：401 场景下 `logout()` 的后端登出请求必然失败，后端 Session 不会被清除。**

#### 6.4.4 路由跳转依赖登出请求成功

`router.push({ name: 'customer.login' })` 位于 `logout()` 方法中登出 HTTP 请求的 `.then()` 回调内（[auth.js:112](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L112)）：

```js
.then((response) => {
    // ...
    router.push({ name: 'customer.login' })  // 仅在请求成功后执行
    resolve(response)
})
```

由于 6.4.3 的分析，登出请求必然失败进入 `.catch()`，`.then()` 永远不会执行，**路由跳转也永远不会发生**。

对比正常登出流程：[TheSiteHeader.vue:260-264](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/layouts/partials/TheSiteHeader.vue#L260-L264) 中在 `.then()` 回调里还有第二个 `router.push`，同样依赖登出请求成功。两处跳转逻辑面临同样的依赖问题。

#### 6.4.5 错误回调中的递归调用风险

`logout()` 的 `.catch()` 回调中调用了 `handleError(err)`（[auth.js:116](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L116)）：

```js
.catch((err) => {
    handleError(err)   // ← 递归入口
    reject(err)
})
```

当登出请求失败时（404 或 405），错误回调重新进入 `handleError`。此时 `err.response.status` 为 404 或 405，不满足 401 识别条件，因此**不会再次调用 `authStore.logout()`**，递归在第一层就会终止。

实际执行路径为：`handleError` → `authStore.logout()` → 登出请求 404 → `.catch(handleError)` → `showError(err.response.data.message)` → 结束。

**递归风险评估：不会产生无限循环**，但会产生一个副作用——登出请求的 404 错误会通过 `showError` 显示一个不可预期的错误通知（内容取决于 404 响应体），且 Promise 被 reject 但未被捕获（`authStore.logout()` 的返回值在 `error-handling.js:27` 中未被 await 或 `.catch()`），可能触发浏览器控制台的 `Unhandled Promise Rejection` 警告。

#### 6.4.6 前端认证状态的清除

`logout()` 方法既未重置 `globalStore.isAppLoaded`，也未清除 `globalStore.currentUser`、`globalStore.companySlug` 等状态。这意味着即使登出请求成功执行了，前端 Pinia Store 中仍然保留着客户数据——**如果用户在登出后手动修改 URL 返回 dashboard，LayoutBasic 会因为 `isAppLoaded = true` 而直接渲染用户态界面**，但此时 API 请求会因 Session 失效而全部返回 401。

#### 6.4.7 401 处理的完整执行路径图

```
后端返回 401（Session 过期 或 enable_portal 被禁用）
            │
            ▼
  进入 handleError(err)        ← 所有 Store 的 .catch() 都调用此函数
            │
            ▼
  判断 err.response.data &&
       (statusText === 'Unauthorized' || data === ' Unauthorized.')
            │
      ┌─────┴────────────────────────────────┐
      │                                      │
   命中（statusText 匹配）              未命中（data 为空字符串
      │                                  且 statusText 不标准）
      ▼                                      │
  showToaster(msg)                            ▼
      │                                  进入 else 分支
      ▼                                  showError(data.message)
  authStore.logout() ← 无参数！               │
      │                                      ▼
      ▼                                  显示错误消息
  POST /undefined/customer/logout        用户停留在当前页面
      │                                  所有 API 持续返回 401
      ▼                                  页面功能瘫痪但不跳转
  404 或 405（公司不存在/方法不匹配）
      │
      ▼
  .catch(handleError)
      │
      ▼
  status=404 → 不满足 401 条件
      │
      ▼
  showError(message) → 显示不相关的错误通知
      │
      ▼
  Promise rejected，Unhandled Rejection
      │
      ▼
  用户停留在当前页面，路由未跳转
  后端 Session 未清除
  前端 Store 状态未重置
```

#### 6.4.8 审计结论

| 检查项 | 结论 | 代码证据 |
|--------|------|----------|
| 401 识别条件是否稳定有效 | ⚠️ 不稳定。`statusText` 判断通常有效但非绝对可靠；`data` 字符串匹配有前导空格 Bug 永远为假；前置条件 `data &&` 在 Session 过期场景可能失败 | [error-handling.js:15-18](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/helpers/error-handling.js#L15-L18) |
| 登出请求是否携带正确的公司参数 | ❌ 否。`logout()` 无参数调用，URL 为 `/undefined/customer/logout`，请求必然 404 | [error-handling.js:27](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/helpers/error-handling.js#L27) 对比 [TheSiteHeader.vue:260](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/layouts/partials/TheSiteHeader.vue#L260) |
| 跳转是否依赖于请求的成功执行 | ❌ 是。`router.push` 在 `.then()` 中，请求失败则不跳转 | [auth.js:112](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L112) |
| 错误回调是否存在递归调用风险 | ⚠️ 理论上存在但实际不会无限循环。404 不满足 401 条件，递归一层即终止；但会产生不相关的错误通知和 Unhandled Promise Rejection | [auth.js:116](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L116) |

## 七、客户单据过滤机制（非草稿）

客户门户的所有数据查询都通过两层过滤来确保数据安全：**客户归属过滤**和**状态过滤（排除草稿）**。客户只能看到自己的、且已发送（非草稿）的业务单据。

### 7.1 两层过滤机制

```
┌─────────────────────────────────────────────────────┐
│  第一层：客户归属过滤                                 │
│  whereCustomer(Auth::guard('customer')->id())       │
│  → 只能看到属于当前客户的单据                         │
└─────────────────────┬───────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────┐
│  第二层：状态过滤（排除草稿）                         │
│  where('status', '<>', 'DRAFT')                     │
│  → 只能看到已发送/已确认的单据，草稿对客户不可见       │
└─────────────────────────────────────────────────────┘
```

### 7.2 发票（Invoice）过滤

文件：[InvoicesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Invoice/InvoicesController.php#L20-L35)

```php
public function index(Request $request)
{
    $invoices = Invoice::with(['items', 'customer', 'creator', 'taxes'])
        ->where('status', '<>', 'DRAFT')
        ->applyFilters($request->all())
        ->whereCustomer(Auth::guard('customer')->id())
        ->latest()
        ->paginateData($limit);

    return InvoiceResource::collection($invoices)
        ->additional(['meta' => [
            'invoiceTotalCount' => Invoice::where('status', '<>', 'DRAFT')
                ->whereCustomer(Auth::guard('customer')->id())
                ->count(),
        ]]);
}
```

关键过滤条件：

| 过滤条件 | 位置 | 作用 |
|----------|------|------|
| `where('status', '<>', 'DRAFT')` | 查询构建器 | 排除草稿状态的发票 |
| `whereCustomer(...)` | 查询构建器 | 限定为当前客户的发票 |
| `where('status', '<>', 'DRAFT')` | meta 统计 | 总数统计同样排除草稿 |

**单据详情查询**（`show` 方法）仅使用 `whereCustomer` 做客户归属校验，**未排除 DRAFT 状态**。客户若能获取到草稿单据的 ID（如通过枚举自增 ID），可直接访问详情接口获取完整草稿数据。详见 7.8 深度审计。

### 7.3 估价单（Estimate）过滤

文件：[EstimatesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Estimate/EstimatesController.php#L20-L47)

```php
$estimates = Estimate::with([...])
    ->where('status', '<>', 'DRAFT')
    ->whereCustomer(Auth::guard('customer')->id())
    ->applyFilters(...)
    ->latest()
    ->paginateData($limit);
```

与发票逻辑一致，估价单同样排除 `DRAFT` 状态并限定客户归属。

### 7.4 付款（Payment）与支出（Expense）

付款和支出的列表查询只做**客户归属过滤**，没有状态过滤：

- **付款**：[PaymentsController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Payment/PaymentsController.php#L24-L25) 使用 `whereCustomer(Auth::guard('customer')->id())`
- **支出**：[ExpensesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Expense/ExpensesController.php#L25) 使用 `whereUser(Auth::guard('customer')->id())`

> **注意**：支出控制器使用的是 `whereUser()` 方法名，而非 `whereCustomer()`，这可能是一个命名不一致的问题，但语义上都是按客户过滤。

### 7.5 Dashboard 统计过滤

文件：[DashboardController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/General/DashboardController.php#L24-L43)

```php
$amountDue = Invoice::whereCustomer($user->id)
    ->where('status', '<>', 'DRAFT')
    ->sum('due_amount');
$invoiceCount = Invoice::whereCustomer($user->id)
    ->where('status', '<>', 'DRAFT')
    ->count();
$estimatesCount = Estimate::whereCustomer($user->id)
    ->where('status', '<>', 'DRAFT')
    ->count();
```

仪表板的所有统计数字（应付金额、发票数、估价单数、最近单据）同样严格遵循"排除草稿 + 限定客户"的双重过滤规则，确保客户看到的数据与列表页一致。

### 7.6 模型 Scope 定义

`whereCustomer` 是定义在各模型上的 Query Scope，用于统一化客户归属过滤逻辑。以 Invoice 为例：

```php
public function scopeWhereCustomer($query, $customer_id)
{
    return $query->where('customer_id', $customer_id);
}
```

这种封装确保了所有查询都通过统一的方法添加客户过滤，减少了遗漏的风险。

### 7.7 数据安全总结

| 数据类型 | 列表客户归属过滤 | 列表排除草稿 | 详情客户归属过滤 | 详情排除草稿 | 状态变更校验 |
|----------|:---------------:|:------------:|:---------------:|:------------:|:------------:|
| Invoice (发票) | ✅ `whereCustomer` | ✅ `status <> DRAFT` | ✅ `whereCustomer` | ❌ **未排除** | - |
| Estimate (估价单) | ✅ `whereCustomer` | ✅ `status <> DRAFT` | ✅ `whereCustomer` | ❌ **未排除** | ❌ **无校验** |
| Payment (付款) | ✅ `whereCustomer` | - | ✅ `whereCustomer` | - | - |
| Expense (支出) | ✅ `whereUser` | - | ✅ `whereUser` | - | - |
| Dashboard 统计 | ✅ | ✅（发票/估价单） | - | - | - |

> **修正说明**：经源码逐行核验，发票与估价单的详情接口均**未排除 DRAFT 状态**，详情页可直接通过 ID 访问草稿数据。此前"详情安全校验 ✅"的结论与代码实际不符，特此修正。

核心设计原则（与实际代码存在偏差）：
1. **预期**：草稿单据对客户完全不可见（列表 + 详情双重屏蔽）
2. **实际**：仅列表屏蔽草稿，详情接口缺失草稿过滤，存在数据泄露面
3. **有效**：横向权限隔离有效——每个客户只能看到自己的数据
4. **有效**：列表与统计口径一致——列表和 Dashboard 均排除草稿

### 7.8 深度审计：草稿单据详情访问与状态变更攻击链

本节基于源码逐行核验，确认草稿单据的访问控制现状，并分析估价单状态接受接口如何被利用以穿越数据访问边界。

#### 7.8.1 发票详情接口：未阻止草稿状态访问

文件：[InvoicesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Invoice/InvoicesController.php#L37-L49)

```php
public function show(Company $company, $id)
{
    $invoice = $company->invoices()
        ->whereCustomer(Auth::guard('customer')->id())
        ->where('id', $id)
        ->first();

    if (! $invoice) {
        return response()->json(['error' => 'invoice_not_found'], 404);
    }

    return new InvoiceResource($invoice);
}
```

查询条件分解：

| 过滤条件 | 是否存在 | 说明 |
|----------|:--------:|------|
| `company_id = ?` | ✅ | 通过 `$company->invoices()` 关系隐式添加 |
| `customer_id = ?` | ✅ | `whereCustomer()` scope |
| `id = ?` | ✅ | 指定单据 ID |
| `status <> 'DRAFT'` | ❌ **缺失** | 列表接口有此条件，详情接口遗漏 |

对比列表接口 [InvoicesController.php:25](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Invoice/InvoicesController.php#L25) 中明确有 `->where('status', '<>', 'DRAFT')`，详情接口存在明显的**过滤条件不一致**问题。

#### 7.8.2 估价单详情接口：同样未阻止草稿状态访问

文件：[EstimatesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Estimate/EstimatesController.php#L55-L67)

```php
public function show(Company $company, $id)
{
    $estimate = $company->estimates()
        ->whereCustomer(Auth::guard('customer')->id())
        ->where('id', $id)
        ->first();

    if (! $estimate) {
        return response()->json(['error' => 'estimate_not_found'], 404);
    }

    return new EstimateResource($estimate);
}
```

与发票详情接口的问题完全一致——三层过滤（公司、客户、ID），缺少状态过滤。

#### 7.8.3 估价单状态接受接口：无状态值校验

文件：[AcceptEstimateController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Estimate/AcceptEstimateController.php#L21-L35)

```php
public function __invoke(Request $request, Company $company, $id)
{
    $estimate = $company->estimates()
        ->whereCustomer(Auth::guard('customer')->id())
        ->where('id', $id)
        ->first();

    if (! $estimate) {
        return response()->json(['error' => 'estimate_not_found'], 404);
    }

    $estimate->update($request->only('status'));

    return new EstimateResource($estimate);
}
```

该接口存在三层安全缺陷：

| 缺陷层级 | 具体问题 | 风险 |
|----------|----------|------|
| 前置状态校验缺失 | 查询时未排除 DRAFT 状态 | 草稿估价单可被修改 |
| 目标状态白名单缺失 | `$request->only('status')` 接受任意字符串 | 可修改为任意状态值 |
| 业务规则校验缺失 | 未检查状态流转是否合法（如 DRAFT→ACCEPTED 是否合理） | 状态机被破坏 |

**前端侧的对应行为**：

文件：[View.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/views/estimates/View.vue#L6-L21)

```vue
<BaseButton
  v-if="estimateStore.selectedViewEstimate.status === 'DRAFT'"
  variant="primary"
  @click="AcceptEstimate"
>
  {{ $t('estimates.accept_estimate') }}
</BaseButton>
```

值得注意的是：前端按钮的显示条件是 `status === 'DRAFT'`——**只有当估价单状态为草稿时才显示"接受"和"拒绝"按钮**。这从业务逻辑角度看非常反常（正常流程应是 SENT/VIEWED 状态下客户才能操作），但客观上与后端详情接口的漏洞形成了"配合"：一旦客户通过详情接口获取到草稿估价单数据，前端会自动渲染操作按钮，使攻击者无需构造自定义 HTTP 请求即可完成状态修改。

#### 7.8.4 攻击链：穿越数据访问边界的完整路径

草稿单据的列表/详情过滤不一致，与状态变更接口的无约束性，共同构成了一条可完整执行的攻击链，穿越了"草稿对客户不可见"的数据访问边界。

```
┌──────────────────────────────────────────────────────────────┐
│  攻击起点：已登录客户门户的合法客户                            │
│  （拥有有效 customer guard Session）                          │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│  步骤 1：枚举/猜测自己名下的草稿单据 ID                         │
│  方式：                                                       │
│   • 自增 ID 遍历（数据库使用自增主键时）                       │
│   • 从已知非草稿单据的 ID 推测相邻 ID                          │
│   • 社会工程学获取（如邮件中的编号线索）                       │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│  步骤 2：调用详情接口获取草稿数据                               │
│  GET /api/v1/{company}/customer/estimates/{draft_id}          │
│  后端查询：                                                    │
│   • company_id = ?  ✅ 通过                                    │
│   • customer_id = ? ✅ 通过（自己名下）                        │
│   • status <> DRAFT ❌ 无此条件，草稿数据被返回                │
│  结果：获取到完整的草稿估价单数据（金额、项目明细等）            │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│  步骤 3（前端自动触发）：渲染操作按钮                            │
│  前端 View.vue 判断：                                          │
│   • v-if="status === 'DRAFT'" → 显示"接受"/"拒绝"按钮        │
│  效果：用户在页面上直接看到操作按钮，可点击交互                  │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│  步骤 4：点击"接受"，调用状态变更接口                            │
│  POST /api/v1/{company}/customer/estimate/{draft_id}/status    │
│  Body: { "status": "ACCEPTED" }                               │
│  后端处理：                                                    │
│   • 查询估价单 → 找到草稿 → ✅                                 │
│   • $request->only('status') → 接受 ACCEPTED → ✅            │
│   • update() → 状态从 DRAFT 变为 ACCEPTED → ✅                │
│  结果：草稿估价单被直接标记为"已接受"，跳过"已发送"环节          │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────┐
│  攻击达成：穿越边界后的效果                                     │
│   • 数据可见性边界被突破：草稿内容对客户可见                    │
│   • 业务状态边界被突破：客户可"自确认"草稿估价单                │
│   • 商家审批流程被绕过：草稿未经发送即变为已接受                │
│   • 后续影响：可能触发基于 ACCEPTED 状态的业务逻辑（如生成      │
│     发票、通知财务等），具体取决于下游业务链的实现              │
└──────────────────────────────────────────────────────────────┘
```

#### 7.8.5 攻击链各环节的脆弱性评估

| 攻击环节 | 依赖条件 | 利用难度 | 影响范围 |
|----------|----------|:--------:|----------|
| 草稿 ID 发现 | 数据库使用自增 ID | 中 | 仅客户自己名下的单据 |
| 草稿详情读取 | 详情接口缺失状态过滤 | 低（有 ID 即可） | 草稿完整数据泄露 |
| 状态变更执行 | 状态接口无校验 | 低（前端按钮可点击） | 业务状态机被破坏 |

**横向越权防护仍然有效**：`whereCustomer(Auth::guard('customer')->id())` 在所有三个接口（列表、详情、状态变更）中都严格存在，确保客户只能访问和修改自己名下的单据，不存在跨客户的数据泄露风险。

**纵向越权（状态边界）防护失效**：客户在自己名下的单据上可以越权操作——访问不应可见的草稿状态、将草稿直接改为已接受，这属于同一资源内的状态边界突破。

## 八、会话生命周期总结

```
1. 客户访问 /{company:slug}/customer/login
       │
       ▼
2. 前端先调用 GET /sanctum/csrf-cookie 获取 XSRF-TOKEN
       │
       ▼
3. LoginController 校验：
   ├── email + company_id 查找客户 ──── 失败 → 401 凭据错误
   ├── 密码哈希比对 ────────────────── 失败 → 401 凭据错误
   └── enable_portal 检查 ──────────── 失败 → 401 门户不可用
       │
       ▼
4. Auth::guard('customer')->login($user) → 建立 Session
       │
       ▼
5. 前端登录成功后跳转 dashboard，LayoutBasic 触发 Bootstrap
   ├── GET /api/v1/{company}/customer/bootstrap
   ├── 加载用户信息、菜单、货币、模块、语言
   └── isAppLoaded = true → 渲染用户态界面
       │
       ▼
6. 后续请求经过中间件栈：
   ├── auth:customer → 验证 Session 有效性 ── 失败 → 401
   └── customer-portal → 验证 enable_portal ── 失败 → 强制登出 + 401
       │
       ▼
7. Controller 层数据过滤：
   ├── whereCustomer() → 限定客户归属
   └── where('status', '<>', 'DRAFT') → 排除草稿单据
       │
       ▼
8. 登出：Auth::guard('customer')->logout() → 销毁 Session
```

## 九、安全边界总结

### 9.1 认证与权限边界

| 边界层 | 机制 | 保护范围 |
|--------|------|----------|
| 认证 Guard 隔离 | `customer` vs `web`/`sanctum` | 防止跨身份体系访问 |
| 公司租户隔离 | `company_id` 限定 + `{company:slug}` 路由绑定 | 防止跨公司数据访问 |
| 门户访问开关 | `enable_portal` 布尔字段（默认 false） | 控制客户是否有权使用门户 |
| 登录时校验 | LoginController 中的 `enable_portal` 检查 | 阻止未授权客户建立会话 |
| 请求时校验 | CustomerPortalMiddleware 的持续检查 | 即时响应权限变更 |
| 密码重置隔离 | `customers` Broker 独立于 `users` Broker | 防止跨体系密码重置令牌混用 |

### 9.2 CSRF 防护边界

| 层级 | 实现 | 作用 |
|------|------|------|
| Cookie 层 | `XSRF-TOKEN` Cookie（`$addHttpCookie = true`） | 前端可读取的 CSRF Token 载体 |
| 请求头层 | Axios 自动添加 `X-XSRF-TOKEN` 请求头 | 向后端证明请求来自可信前端 |
| 中间件层 | `PreventRequestForgery` 校验请求头与会话 Token | 防止跨站请求伪造 |
| 豁免配置 | `login` 路由排除 CSRF 校验 | 仅匹配 Admin `/login`，**客户登录不豁免** |

> **审计修正**：`$except = ['login']` 使用 `request()->is()` 前缀匹配，仅豁免 Admin 登录路径 `/login`。客户登录路径 `/{company:slug}/customer/login` **不在豁免范围内**。前端通过先调用 `GET /sanctum/csrf-cookie` 获取 CSRF Cookie，使得后续登录 POST 请求能携带有效 Token。

### 9.3 数据访问边界

| 数据类型 | 列表过滤 | 详情过滤 | 状态变更 |
|----------|----------|----------|----------|
| 发票 | ✅ `whereCustomer` + `status <> DRAFT` | ⚠️ `whereCustomer`（**未排除草稿**） | - |
| 估价单 | ✅ `whereCustomer` + `status <> DRAFT` | ⚠️ `whereCustomer`（**未排除草稿**） | ❌ **任意状态可修改** |
| 付款 | ✅ `whereCustomer` | ✅ `whereCustomer` | - |
| 支出 | ✅ `whereUser` | ✅ `whereUser` | - |
| Dashboard 统计 | ✅ 双重过滤一致应用 | - | - |

> **审计发现**：
> 1. 发票与估价单详情接口缺失 `status <> DRAFT` 过滤，若客户得知自己草稿单据的 ID（自增 ID 可被枚举），可访问详情。
> 2. 估价单状态变更接口 `POST /estimate/{id}/status` 未校验当前状态，也未限制目标状态的合法性，理论上客户可将草稿改为 `ACCEPTED`。
> 3. 以上漏洞仅限于客户**自己名下**的单据，`whereCustomer` 严格防止横向越权。

### 9.4 前端安全边界

| 机制 | 预期行为 | 实际行为 | 状态 |
|------|----------|----------|:----:|
| 401 响应体匹配 | 识别后端返回的 `'Unauthorized.'` | 前端判断为 `' Unauthorized.'`（多一个前导空格），**条件永远不成立** | ❌ |
| 被动认证跳转 | 收到 401 后跳转登录页 | 跳转依赖登出请求 `.then()`，但请求 URL 为 `/undefined/customer/logout`（未传 company 参数），永远失败 | ❌ |
| 后端 Session 清除 | 调用后端登出接口 | 登出请求 404，后端 Session 可能未清除 | ❌ |
| 前端状态清除 | 清除 Pinia Store 中的用户状态 | `logout()` 方法未显式清除任何 Store 状态 | ❌ |
| 布局级加载控制 | `isAppLoaded` 控制内容渲染 | 正常工作 | ✅ |
| Session Cookie 安全 | `http_only` + `same_site=lax` | 正常工作（Laravel 默认） | ✅ |
| 错误递归防护 | 防止登出失败引发无限循环 | `.catch` 中再次调用 `handleError()`，可能导致栈溢出 | ❌ |

### 9.5 深度审计问题清单汇总

| 编号 | 位置 | 问题描述 | 严重程度 | 建议修复 |
|------|------|----------|:--------:|----------|
| F-001 | [error-handling.js:18](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/helpers/error-handling.js#L18) | 401 响应体字符串前多余空格，匹配条件永假 | 高 | 将 `' Unauthorized.'` 改为 `'Unauthorized.'`，或改用 `err.response.status === 401` 数字判断 |
| F-002 | [error-handling.js:27](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/helpers/error-handling.js#L27) | `authStore.logout()` 调用未传 company 参数，URL 含 `undefined` | 高 | 调用时传入当前 company slug，或在 `logout()` 内部从路由/Store 获取 |
| F-003 | [auth.js:112](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L112) | `router.push` 在 `.then()` 中，请求失败不跳转 | 高 | 跳转逻辑移入 `.finally()`，或不依赖请求结果直接跳转 |
| F-004 | [auth.js:102-120](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L102-L120) | `logout()` 未清除前端认证状态 | 中 | 在登出逻辑中重置 `globalStore.isAppLoaded`、`currentUser` 等状态 |
| F-005 | [auth.js:116-117](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L116-L117) | `.catch` 中递归调用 `handleError`，404 不满足 401 条件故不会死循环，但会产生不相关的错误通知和 Unhandled Promise Rejection | 中 | 登出失败直接跳转，不再调用 `handleError` |
| B-001 | [InvoicesController.php:39-42](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Invoice/InvoicesController.php#L39-L42) | 发票详情查询未排除 DRAFT 状态 | 中 | 添加 `->where('status', '<>', Invoice::STATUS_DRAFT)` |
| B-002 | [EstimatesController.php:57-60](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Estimate/EstimatesController.php#L57-L60) | 估价单详情查询未排除 DRAFT 状态 | 中 | 添加 `->where('status', '<>', Estimate::STATUS_DRAFT)` |
| B-003 | [AcceptEstimateController.php:32](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Estimate/AcceptEstimateController.php#L32) | 估价单状态变更无白名单校验，可改任意值 | 高 | 校验当前状态非 DRAFT 且目标状态仅允许 `ACCEPTED`/`REJECTED` |
