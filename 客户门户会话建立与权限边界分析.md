# 客户门户（Customer Portal）会话建立与权限边界分析

## 一、架构概览

InvoiceShelf 采用**双 Guard 隔离架构**，将管理后台（Admin）与客户门户（Customer Portal）的认证体系完全分离。Admin 使用 `web` / `sanctum` Guard，Customer Portal 使用独立的 `customer` Guard，二者共享同一 Laravel 应用但通过不同的认证守卫、中间件和路由前缀实现权限隔离。

```
┌──────────────────────────────────────────────────────────┐
│                    Laravel Application                    │
│                                                          │
│  ┌─────────────────────┐    ┌─────────────────────────┐  │
│  │   Admin Guard (web)  │    │  Customer Guard (customer)│ │
│  │   Provider: users    │    │  Provider: customers      │ │
│  │   Model: User        │    │  Model: Customer          │ │
│  │   Driver: session    │    │  Driver: session          │ │
│  └─────────┬───────────┘    └───────────┬──────────────┘  │
│            │                            │                 │
│   auth:sanctum + company + bouncer   auth:customer        │
│            │                      + customer-portal       │
│            │                            │                 │
│  ┌─────────▼───────────┐    ┌───────────▼──────────────┐  │
│  │   Admin Routes       │    │  Customer Portal Routes   │ │
│  │   /api/v1/*          │    │  /api/v1/{company:slug}   │ │
│  │                      │    │  /customer/*              │ │
│  └──────────────────────┘    └───────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

## 二、认证配置层

### 2.1 Guard 定义

文件：[auth.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/config/auth.php#L41-L56)

```php
'guards' => [
    'web' => [
        'driver' => 'session',
        'provider' => 'users',
    ],
    'api' => [
        'driver' => 'token',
        'provider' => 'users',
        'hash' => false,
    ],
    'customer' => [
        'driver' => 'session',
        'provider' => 'customers',
        'hash' => false,
    ],
],
```

关键设计：

- `customer` Guard 使用 `session` 驱动，意味着客户门户通过 Cookie/Session 机制维持会话状态
- `customer` Guard 绑定到 `customers` Provider，该 Provider 使用 `Customer` 模型
- 这确保了 Admin 的 `web` Guard 和 Customer 的 `customer` Guard 的 Session 数据互不干扰

### 2.2 Provider 定义

文件：[auth.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/config/auth.php#L58-L67)

```php
'providers' => [
    'users' => [
        'driver' => 'eloquent',
        'model' => User::class,
    ],
    'customers' => [
        'driver' => 'eloquent',
        'model' => Customer::class,
    ],
],
```

### 2.3 密码重置 Broker

文件：[auth.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/config/auth.php#L88-L101)

```php
'passwords' => [
    'users' => [
        'provider' => 'users',
        'table' => 'password_reset_tokens',
        'expire' => 60,
        'throttle' => 60,
    ],
    'customers' => [
        'provider' => 'customers',
        'table' => 'password_reset_tokens',
        'expire' => 60,
        'throttle' => 60,
    ],
],
```

客户使用独立的 `customers` 密码重置 Broker，与 Admin 的 `users` Broker 分离，保证密码重置令牌不会跨身份体系混用。

## 三、会话建立流程

### 3.1 登录入口

客户门户的登录有两个路由入口：

**Web 路由（SPA 入口）：**

文件：[web.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L45)

```php
Route::post('/{company:slug}/customer/login', CustomerLoginController::class);
```

**API 路由（认证子组）：**

文件：[api.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/api.php#L494-L501)

```php
Route::prefix('/{company:slug}/customer')->group(function () {
    Route::prefix('auth')->group(function () {
        Route::post('password/email', [AuthForgotPasswordController::class, 'sendResetLinkEmail']);
        Route::post('reset/password', [AuthResetPasswordController::class, 'reset'])->name('customer.password.reset');
    });
    // ...
});
```

注意：登录路由（`POST /{company:slug}/customer/login`）位于 `web.php` 中，而密码重置路由位于 `api.php` 中。

### 3.2 登录控制器

文件：[LoginController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/LoginController.php#L21-L44)

```php
public function __invoke(CustomerLoginRequest $request, Company $company)
{
    $user = Customer::where('email', $request->email)
        ->where('company_id', $company->id)
        ->first();

    if (! $user || ! Hash::check($request->password, $user->password)) {
        throw ValidationException::withMessages([
            'email' => ['The provided credentials are incorrect.'],
        ]);
    }

    if (! $user->enable_portal) {
        throw ValidationException::withMessages([
            'email' => ['Customer portal not available for this user.'],
        ]);
    }

    Auth::guard('customer')->login($user);

    return response()->json([
        'success' => true,
    ]);
}
```

登录流程的三重校验：

| 步骤 | 校验内容 | 失败行为 |
|------|----------|----------|
| 1 | 通过 `email` + `company_id` 查找客户记录 | 抛出 `ValidationException`（凭据错误） |
| 2 | 密码哈希比对 `Hash::check()` | 抛出 `ValidationException`（凭据错误） |
| 3 | `enable_portal` 布尔字段检查 | 抛出 `ValidationException`（门户不可用） |

关键细节：查询时同时限定了 `company_id`，即**客户必须在指定公司下**才能登录，这构成了公司级别的租户隔离。

### 3.3 登出

文件：[web.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/web.php#L47-L49)

```php
Route::post('/{company:slug}/customer/logout', function () {
    Auth::guard('customer')->logout();
});
```

直接调用 `customer` Guard 的 `logout()` 方法清除 Session。

### 3.4 密码重置

- **忘记密码**：[ForgotPasswordController](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/ForgotPasswordController.php) 使用 `Password::broker('customers')` 发送重置邮件
- **重置密码**：[ResetPasswordController](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/ResetPasswordController.php) 同样使用 `customers` Broker，重置成功后重定向到 `RouteServiceProvider::CUSTOMER_HOME`（`/customer/dashboard`）

## 四、权限边界实现机制

### 4.1 `enable_portal` 字段 — 门户访问的总开关

文件：[Customer.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Models/Customer.php#L46-L51)

```php
protected function casts(): array
{
    return [
        'enable_portal' => 'boolean',
    ];
}
```

数据库迁移（[change_enable_portal_field](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/database/migrations/2021_12_21_102521_change_enable_portal_field_of_customers_table.php#L15-L17)）：

```php
Schema::table('customers', function (Blueprint $table) {
    $table->boolean('enable_portal')->default(false)->change();
});
```

`enable_portal` 默认为 `false`，即新创建的客户默认**没有**门户访问权限，必须由管理员显式启用。这是一个 "deny by default" 的安全策略。

### 4.2 双重 `enable_portal` 校验

`enable_portal` 在两个位置被检查，形成**双重防线**：

#### 第一层：登录时校验

文件：[LoginController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/LoginController.php#L33-L37)

```php
if (! $user->enable_portal) {
    throw ValidationException::withMessages([
        'email' => ['Customer portal not available for this user.'],
    ]);
}
```

在凭证验证通过后、Session 建立前，检查 `enable_portal`。如果为 `false`，拒绝登录。

#### 第二层：中间件持续校验

文件：[CustomerPortalMiddleware.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/CustomerPortalMiddleware.php#L19-L30)

```php
public function handle(Request $request, Closure $next): Response
{
    $user = Auth::guard('customer')->user();

    if (! $user->enable_portal) {
        Auth::guard('customer')->logout();

        return response('Unauthorized.', 401);
    }

    return $next($request);
}
```

在**每次请求**时检查已认证客户的 `enable_portal` 状态。如果管理员在客户登录后禁用了门户权限，该客户在下次请求时会被**强制登出**并返回 401。

这种双重校验的设计确保了：
- 未授权客户无法建立会话（第一层）
- 已登录客户的权限变更能即时生效（第二层）

### 4.3 中间件注册与路由绑定

#### 中间件别名注册

文件：[app.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/bootstrap/app.php#L70-L84)

```php
$middleware->alias([
    'auth' => Authenticate::class,
    'customer' => CustomerRedirectIfAuthenticated::class,
    'customer-guest' => CustomerGuest::class,
    'customer-portal' => CustomerPortalMiddleware::class,
    // ...
]);
```

与客户门户相关的中间件别名有三个：

| 别名 | 中间件类 | 作用 |
|------|----------|------|
| `customer` | `CustomerRedirectIfAuthenticated` | 已认证客户访问 guest 路由时重定向 |
| `customer-guest` | `CustomerGuest` | 委托给 `CustomerRedirectIfAuthenticated` |
| `customer-portal` | `CustomerPortalMiddleware` | 校验 `enable_portal` 权限 |

#### 路由中间件组

文件：[api.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/routes/api.php#L506)

```php
Route::middleware(['auth:customer', 'customer-portal'])->group(function () {
    // 所有客户门户受保护的路由
});
```

客户门户的受保护路由使用**中间件栈** `auth:customer` + `customer-portal`：
1. `auth:customer`：确保请求已通过 `customer` Guard 认证（未认证返回 401）
2. `customer-portal`：在认证通过后，再次校验 `enable_portal` 权限

### 4.4 公司级租户隔离

文件：[LoginController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Auth/LoginController.php#L23-L25)

```php
$user = Customer::where('email', $request->email)
    ->where('company_id', $company->id)
    ->first();
```

登录查询同时限定 `company_id`，路由参数 `{company:slug}` 通过 Laravel 的 Route Model Binding 自动解析为 `Company` 模型实例。这确保了：

- 客户只能在其所属公司的门户下登录
- 即使不同公司存在相同 email 的客户，也不会跨公司认证

### 4.5 Guard 隔离

Admin 和 Customer Portal 使用完全不同的认证 Guard：

| 维度 | Admin | Customer Portal |
|------|-------|-----------------|
| Guard 名称 | `web` / `sanctum` | `customer` |
| 用户模型 | `User` | `Customer` |
| Session 存储 | 独立 Session | 独立 Session |
| 登录入口 | `POST /login` | `POST /{company:slug}/customer/login` |
| 权限系统 | Bouncer (角色+能力) | `enable_portal` 布尔开关 |

这意味着：
- Admin 的 Session 不会赋予客户门户的访问权限
- Customer 的 Session 不会赋予管理后台的访问权限
- 两者可以同时在线而互不影响

### 4.6 与 Admin 权限系统的对比

Admin 端使用了 Silber/Bouncer 的角色与能力系统（RBAC），通过 [ScopeBouncer](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/ScopeBouncer.php) 中间件按公司租户划定作用域：

```php
$this->bouncer->scope()->to($tenantId);
```

而 Customer Portal 不使用 Bouncer，而是采用更简单的 `enable_portal` 布尔开关作为权限边界。这符合业务逻辑：客户不需要细粒度的角色权限，只需要一个"能否访问门户"的开关。

### 4.7 PDF 访问的特殊权限

文件：[PdfMiddleware.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/PdfMiddleware.php#L19-L26)

```php
if (Auth::guard('web')->check() || Auth::guard('sanctum')->check() || Auth::guard('customer')->check()) {
    return $next($request);
}
```

PDF 中间件接受三种 Guard 的任一认证，即 Admin 用户和已认证客户都能访问 PDF 资源。但客户门户的 PDF 端点还额外支持通过 `email_log:token` 无认证访问（用于邮件中的 PDF 链接）。

## 五、前端 CSRF 防护机制

客户门户是基于 Laravel + Vue 的 SPA 应用，采用了 Laravel 标准的 **Cookie-based CSRF 防护**方案，通过 `XSRF-TOKEN` Cookie 与 `X-XSRF-TOKEN` 请求头的配对验证来防御跨站请求伪造攻击。

### 5.1 后端 CSRF 中间件配置

文件：[PreventRequestForgery.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Middleware/PreventRequestForgery.php#L7-L23)

```php
class PreventRequestForgery extends Middleware
{
    protected $addHttpCookie = true;

    protected $except = [
        'login',
    ];
}
```

关键配置：

| 配置项 | 值 | 含义 |
|--------|----|------|
| `$addHttpCookie` | `true` | 响应时自动添加 `XSRF-TOKEN` Cookie，供前端 JS 读取 |
| `$except` | `['login']` | 登录接口豁免 CSRF 校验（因为登录前还没有有效 Token） |

`$addHttpCookie = true` 是前端能够读取 CSRF Token 的前提——Laravel 会在响应中设置一个名为 `XSRF-TOKEN` 的 Cookie，前端 JavaScript 可以读取该 Cookie 并在后续请求中以 `X-XSRF-TOKEN` 头发送回去。

### 5.2 前端 Axios 配置

文件：[http/index.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/http/index.js#L1-L28)

```js
const instance = axios.create({
  withCredentials: true,
  headers: {
    common: {
      'X-Requested-With': 'XMLHttpRequest',
    },
  },
})
```

CSRF 防护的前端配置要点：

1. **`withCredentials: true`**：允许 Axios 在跨域请求中携带 Cookie（包括 Session Cookie 和 XSRF-TOKEN Cookie）。这是 Laravel SPA 认证模式的核心配置。
2. **`X-Requested-With: XMLHttpRequest`**：标识请求为 AJAX 请求，Laravel 会据此判断是否以 AJAX 方式响应（如返回 JSON 而非重定向）。

> **注意**：代码中的 Axios 实例并未显式设置 `X-XSRF-TOKEN` 请求头。这是因为 Axios 默认会自动从 `XSRF-TOKEN` Cookie 中读取值并添加到 `X-XSRF-TOKEN` 请求头中——只要 `withCredentials` 为 `true` 且 Cookie 存在，Axios 会自动处理。

### 5.3 CSRF Token 获取流程

客户门户登录前需要先获取 CSRF Cookie：

文件：[auth.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/auth.js#L19-L43)

```js
login(data) {
  return new Promise((resolve, reject) => {
    http.get('/sanctum/csrf-cookie').then((response) => {
      if (response) {
        http
          .post(`/${data.company}/customer/login`, data)
          .then(...)
      }
    })
  })
}
```

完整的 CSRF 初始化流程：

```
┌──────────────────────────────────────────────────────────┐
│  1. 前端调用 GET /sanctum/csrf-cookie                     │
│     （触发 Laravel 设置 XSRF-TOKEN Cookie 和 Session Cookie）│
└──────────────────────┬───────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  2. 浏览器存储两个 Cookie：                                │
│     • 应用名_session（Session ID，HttpOnly）              │
│     • XSRF-TOKEN（CSRF Token，可被 JS 读取）              │
└──────────────────────┬───────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  3. 前端发送 POST /{company}/customer/login               │
│     Axios 自动：                                          │
│     • 携带 Cookie（withCredentials: true）               │
│     • 从 XSRF-TOKEN Cookie 读取值并添加 X-XSRF-TOKEN 头  │
└──────────────────────┬───────────────────────────────────┘
                       ▼
┌──────────────────────────────────────────────────────────┐
│  4. 后端 PreventRequestForgery 中间件校验：               │
│     比较请求头中的 X-XSRF-TOKEN 与会话中的 CSRF Token     │
│     匹配 → 通过；不匹配 → 419 Page Expired               │
└──────────────────────────────────────────────────────────┘
```

### 5.4 Blade 模板中的备用 CSRF

文件：[app.blade.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/views/app.blade.php#L17)

```html
<meta name="csrf-token" content="{{ csrf_token() }}">
```

Blade 模板中还以 `<meta>` 标签的形式注入了 CSRF Token，作为传统表单提交或非 Axios 请求时的备用方案。

## 六、Bootstrap 初始化流程

客户门户的前端应用在进入用户态（已登录视图）之前，会调用 Bootstrap 接口获取当前客户的完整上下文信息，完成应用初始化。

### 6.1 前端初始化入口

文件：[LayoutBasic.vue](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/layouts/LayoutBasic.vue#L17-L35)

```vue
<script setup>
const globalStore = useGlobalStore()
const route = useRoute()
const isAppLoaded = computed(() => globalStore.isAppLoaded)

loadData()

async function loadData() {
  await globalStore.bootstrap(route.params.company)
}
</script>
```

`LayoutBasic` 是客户门户所有需要认证的页面的根布局组件。它在组件挂载时立即调用 `bootstrap()`，只有当 `isAppLoaded` 为 `true` 时才渲染实际内容，否则页面处于空白状态。

> **注意**：前端路由的 `meta.requiresAuth` 标记在全局 `router.beforeEach` 中并未实际校验客户的认证状态（只处理了 Admin 端的 ability 检查）。客户门户的认证检查实际上是通过 Bootstrap API 请求来隐式完成的——如果未登录，后端会返回 401，前端错误处理器会自动触发登出并跳转登录页。

### 6.2 Bootstrap Store 实现

文件：[global.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/stores/global.js#L20-L46)

```js
bootstrap(data) {
  this.companySlug = data
  const userStore = useUserStore()
  return new Promise((resolve, reject) => {
    http
      .get(`/api/v1/${data}/customer/bootstrap`)
      .then((response) => {
        this.currentUser = response.data.data
        this.mainMenu = response.data.meta.menu
        this.currency = response.data.data.currency
        this.enabledModules = response.data.meta.modules
        Object.assign(userStore.userForm, response.data.data)
        global.locale.value = response.data.meta.current_company_language || 'en'
        this.isAppLoaded = true
        resolve(response)
      })
  })
}
```

Bootstrap 接口返回的数据会填充到多个 Store 中：

| 数据项 | 存储位置 | 用途 |
|--------|----------|------|
| `data`（客户信息） | `globalStore.currentUser` + `userStore.userForm` | 当前登录客户的完整资料 |
| `meta.menu` | `globalStore.mainMenu` | 客户门户侧边栏/导航菜单 |
| `data.currency` | `globalStore.currency` | 客户默认货币 |
| `meta.modules` | `globalStore.enabledModules` | 已启用的模块列表 |
| `meta.current_company_language` | `i18n.global.locale` | 公司语言设置 |

### 6.3 后端 Bootstrap 接口

文件：[BootstrapController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/General/BootstrapController.php#L21-L41)

```php
public function __invoke(Request $request)
{
    $customer = Auth::guard('customer')->user();

    foreach (\Menu::get('customer_portal_menu')->items->toArray() as $data) {
        $menu[] = [
            'title' => $data->title,
            'link' => $data->link->path['url'],
        ];
    }

    return (new CustomerResource($customer))
        ->additional(['meta' => [
            'menu' => $menu,
            'current_customer_currency' => Currency::find($customer->currency_id),
            'modules' => Module::where('enabled', true)->pluck('name'),
            'current_company_language' => CompanySetting::getSetting('language', $customer->company_id),
        ]]);
}
```

Bootstrap 接口的职责：

1. 通过 `Auth::guard('customer')->user()` 获取当前认证客户
2. 从 `lavary/laravel-menu` 获取 `customer_portal_menu` 菜单配置
3. 查询客户货币、启用模块、公司语言等全局配置
4. 以 `CustomerResource` 格式返回，并附加 `meta` 元数据

该接口受 `auth:customer` + `customer-portal` 双重中间件保护，确保只有已登录且启用了门户权限的客户才能调用。

### 6.4 认证失败的降级处理

文件：[error-handling.js](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/resources/scripts/customer/helpers/error-handling.js#L15-L27)

```js
if (
  err.response.data &&
  (err.response.statusText === 'Unauthorized' ||
    err.response.data === ' Unauthorized.')
) {
  showToaster(msg)
  authStore.logout()
}
```

当 Bootstrap 请求或任何 API 请求返回 401 Unauthorized 时，前端全局错误处理器会：
1. 显示错误通知
2. 调用 `authStore.logout()` 触发登出流程
3. 跳转到客户登录页

这形成了一个**被动认证检查**机制——不依赖前端路由守卫，而是靠后端返回 401 时的统一处理来保证安全。

## 七、客户单据过滤机制（非草稿）

客户门户的所有数据查询都通过两层过滤来确保数据安全：**客户归属过滤**和**状态过滤（排除草稿）**。客户只能看到自己的、且已发送（非草稿）的业务单据。

### 7.1 两层过滤机制

```
┌─────────────────────────────────────────────────────┐
│  第一层：客户归属过滤                                 │
│  whereCustomer(Auth::guard('customer')->id())       │
│  → 只能看到属于当前客户的单据                         │
└─────────────────────┬───────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────┐
│  第二层：状态过滤（排除草稿）                         │
│  where('status', '<>', 'DRAFT')                     │
│  → 只能看到已发送/已确认的单据，草稿对客户不可见       │
└─────────────────────────────────────────────────────┘
```

### 7.2 发票（Invoice）过滤

文件：[InvoicesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Invoice/InvoicesController.php#L20-L35)

```php
public function index(Request $request)
{
    $invoices = Invoice::with(['items', 'customer', 'creator', 'taxes'])
        ->where('status', '<>', 'DRAFT')
        ->applyFilters($request->all())
        ->whereCustomer(Auth::guard('customer')->id())
        ->latest()
        ->paginateData($limit);

    return InvoiceResource::collection($invoices)
        ->additional(['meta' => [
            'invoiceTotalCount' => Invoice::where('status', '<>', 'DRAFT')
                ->whereCustomer(Auth::guard('customer')->id())
                ->count(),
        ]]);
}
```

关键过滤条件：

| 过滤条件 | 位置 | 作用 |
|----------|------|------|
| `where('status', '<>', 'DRAFT')` | 查询构建器 | 排除草稿状态的发票 |
| `whereCustomer(...)` | 查询构建器 | 限定为当前客户的发票 |
| `where('status', '<>', 'DRAFT')` | meta 统计 | 总数统计同样排除草稿 |

**单据详情查询**（`show` 方法）同样使用 `whereCustomer` 做归属校验，但未显式排除 DRAFT 状态——不过由于列表不展示草稿，客户无法通过常规途径获取草稿 ID，且若尝试访问会返回 404。

### 7.3 估价单（Estimate）过滤

文件：[EstimatesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Estimate/EstimatesController.php#L20-L47)

```php
$estimates = Estimate::with([...])
    ->where('status', '<>', 'DRAFT')
    ->whereCustomer(Auth::guard('customer')->id())
    ->applyFilters(...)
    ->latest()
    ->paginateData($limit);
```

与发票逻辑一致，估价单同样排除 `DRAFT` 状态并限定客户归属。

### 7.4 付款（Payment）与支出（Expense）

付款和支出的列表查询只做**客户归属过滤**，没有状态过滤：

- **付款**：[PaymentsController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Payment/PaymentsController.php#L24-L25) 使用 `whereCustomer(Auth::guard('customer')->id())`
- **支出**：[ExpensesController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/Expense/ExpensesController.php#L25) 使用 `whereUser(Auth::guard('customer')->id())`

> **注意**：支出控制器使用的是 `whereUser()` 方法名，而非 `whereCustomer()`，这可能是一个命名不一致的问题，但语义上都是按客户过滤。

### 7.5 Dashboard 统计过滤

文件：[DashboardController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/50-InvoiceShelf/app/Http/Controllers/V1/Customer/General/DashboardController.php#L24-L43)

```php
$amountDue = Invoice::whereCustomer($user->id)
    ->where('status', '<>', 'DRAFT')
    ->sum('due_amount');
$invoiceCount = Invoice::whereCustomer($user->id)
    ->where('status', '<>', 'DRAFT')
    ->count();
$estimatesCount = Estimate::whereCustomer($user->id)
    ->where('status', '<>', 'DRAFT')
    ->count();
```

仪表板的所有统计数字（应付金额、发票数、估价单数、最近单据）同样严格遵循"排除草稿 + 限定客户"的双重过滤规则，确保客户看到的数据与列表页一致。

### 7.6 模型 Scope 定义

`whereCustomer` 是定义在各模型上的 Query Scope，用于统一化客户归属过滤逻辑。以 Invoice 为例：

```php
public function scopeWhereCustomer($query, $customer_id)
{
    return $query->where('customer_id', $customer_id);
}
```

这种封装确保了所有查询都通过统一的方法添加客户过滤，减少了遗漏的风险。

### 7.7 数据安全总结

| 数据类型 | 客户归属过滤 | 排除草稿 | 详情安全校验 |
|----------|:------------:|:--------:|:------------:|
| Invoice (发票) | ✅ `whereCustomer` | ✅ `status <> DRAFT` | ✅ `whereCustomer` + 404 |
| Estimate (估价单) | ✅ `whereCustomer` | ✅ `status <> DRAFT` | ✅ `whereCustomer` + 404 |
| Payment (付款) | ✅ `whereCustomer` | ❌（无草稿概念） | ✅ `whereCustomer` + 404 |
| Expense (支出) | ✅ `whereUser` | ❌（无草稿概念） | ✅ `whereUser` + 404 |
| Dashboard 统计 | ✅ | ✅（发票/估价单） | - |

核心设计原则：
1. **默认最小可见性**：草稿单据对客户完全不可见
2. **横向权限隔离**：每个客户只能看到自己的数据
3. **防御性查询**：详情查询不依赖路由参数 ID 直接查找，必须加上 `whereCustomer` 二次校验

## 八、会话生命周期总结

```
1. 客户访问 /{company:slug}/customer/login
       │
       ▼
2. 前端先调用 GET /sanctum/csrf-cookie 获取 XSRF-TOKEN
       │
       ▼
3. LoginController 校验：
   ├── email + company_id 查找客户 ──── 失败 → 401 凭据错误
   ├── 密码哈希比对 ────────────────── 失败 → 401 凭据错误
   └── enable_portal 检查 ──────────── 失败 → 401 门户不可用
       │
       ▼
4. Auth::guard('customer')->login($user) → 建立 Session
       │
       ▼
5. 前端登录成功后跳转 dashboard，LayoutBasic 触发 Bootstrap
   ├── GET /api/v1/{company}/customer/bootstrap
   ├── 加载用户信息、菜单、货币、模块、语言
   └── isAppLoaded = true → 渲染用户态界面
       │
       ▼
6. 后续请求经过中间件栈：
   ├── auth:customer → 验证 Session 有效性 ── 失败 → 401
   └── customer-portal → 验证 enable_portal ── 失败 → 强制登出 + 401
       │
       ▼
7. Controller 层数据过滤：
   ├── whereCustomer() → 限定客户归属
   └── where('status', '<>', 'DRAFT') → 排除草稿单据
       │
       ▼
8. 登出：Auth::guard('customer')->logout() → 销毁 Session
```

## 九、安全边界总结

### 9.1 认证与权限边界

| 边界层 | 机制 | 保护范围 |
|--------|------|----------|
| 认证 Guard 隔离 | `customer` vs `web`/`sanctum` | 防止跨身份体系访问 |
| 公司租户隔离 | `company_id` 限定 + `{company:slug}` 路由绑定 | 防止跨公司数据访问 |
| 门户访问开关 | `enable_portal` 布尔字段（默认 false） | 控制客户是否有权使用门户 |
| 登录时校验 | LoginController 中的 `enable_portal` 检查 | 阻止未授权客户建立会话 |
| 请求时校验 | CustomerPortalMiddleware 的持续检查 | 即时响应权限变更 |
| 密码重置隔离 | `customers` Broker 独立于 `users` Broker | 防止跨体系密码重置令牌混用 |

### 9.2 CSRF 防护边界

| 层级 | 实现 | 作用 |
|------|------|------|
| Cookie 层 | `XSRF-TOKEN` Cookie（`$addHttpCookie = true`） | 前端可读取的 CSRF Token 载体 |
| 请求头层 | Axios 自动添加 `X-XSRF-TOKEN` 请求头 | 向后端证明请求来自可信前端 |
| 中间件层 | `PreventRequestForgery` 校验请求头与会话 Token | 防止跨站请求伪造 |
| 豁免配置 | `login` 路由排除 CSRF 校验 | 登录前无有效 Token 时的入口 |

### 9.3 数据访问边界

| 数据类型 | 过滤机制 | 安全效果 |
|----------|----------|----------|
| 发票/估价单 | `whereCustomer()` + `where('status', '<>', 'DRAFT')` | 只能看自己的、非草稿的单据 |
| 付款/支出 | `whereCustomer()` / `whereUser()` | 只能看自己的交易记录 |
| 单据详情 | `whereCustomer()` + 404 兜底 | 防止通过 ID 遍历访问他人数据 |
| Dashboard 统计 | 双重过滤一致应用 | 统计数字与列表数据口径一致 |

### 9.4 前端安全边界

| 机制 | 实现方式 | 作用 |
|------|----------|------|
| 被动认证检查 | Bootstrap API + 全局 401 错误处理 | 未登录时自动跳转登录页 |
| 布局级加载控制 | `isAppLoaded` 控制内容渲染 | 初始化完成前不暴露用户态 UI |
| Session Cookie | `http_only` + `same_site=lax` | 防止 XSS 窃取 Session |
