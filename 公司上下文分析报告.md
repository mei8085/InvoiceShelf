# InvoiceShelf 公司上下文分析报告

## 1. 公司设置存储结构

### 1.1 核心数据表

InvoiceShelf 采用三级设置存储架构，实现了系统级、公司级和用户级的配置隔离：

#### 1.1.1 `companies` 表 - 公司主体信息

**迁移文件**: `database/migrations/2014_10_11_071840_create_companies_table.php`

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | increments | 主键 |
| `name` | string | 公司名称 |
| `logo` | string, nullable | Logo 路径（已弃用，改用 media library） |
| `unique_hash` | string, nullable | 公司唯一哈希标识（用于客户门户 URL） |
| `owner_id` | integer, nullable | 公司所有者 ID（后续迁移添加） |
| `created_at` | timestamp | 创建时间 |
| `updated_at` | timestamp | 更新时间 |

#### 1.1.2 `company_settings` 表 - 公司级配置

**迁移文件**: `database/migrations/2019_09_26_145012_create_company_settings_table.php`

采用键值对（Key-Value）存储模式，支持灵活的配置扩展：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | increments | 主键 |
| `option` | string | 配置键名 |
| `value` | text | 配置值（后续迁移从 string 改为 text） |
| `company_id` | integer, unsigned, nullable | 外键关联 companies 表 |
| `created_at` | timestamp | 创建时间 |
| `updated_at` | timestamp | 更新时间 |

**索引与约束**:
- `company_id` 外键关联 `companies.id`，支持级联删除

#### 1.1.3 `user_company` 表 - 用户公司关联

**迁移文件**: `database/migrations/2021_07_01_060700_create_user_company_table.php`

实现用户与公司的多对多关系：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | id | 主键 |
| `user_id` | unsignedInteger, nullable | 用户 ID，外键关联 users 表 |
| `company_id` | unsignedInteger, nullable | 公司 ID，外键关联 companies 表 |
| `created_at` | timestamp | 创建时间 |
| `updated_at` | timestamp | 更新时间 |

**级联操作**: `ON DELETE CASCADE`，删除用户或公司时自动解除关联

#### 1.1.4 其他相关表

| 表名 | 说明 | 关联方式 |
|------|------|----------|
| `settings` | 系统全局设置 | 无 company_id，适用于所有公司 |
| `user_settings` | 用户个人设置 | 按 user_id 隔离 |

### 1.2 模型层设计

#### 1.2.1 Company 模型 (`app/Models/Company.php`)

**核心关系**:
```php
public function settings(): HasMany          // 公司设置
public function users(): BelongsToMany       // 关联用户
public function owner(): BelongsTo           // 公司所有者
public function customers(): HasMany         // 客户
public function invoices(): HasMany          // 发票
public function estimates(): HasMany         // 报价单
// ... 其他业务数据关系
```

**关键方法**:
- `setupDefaultData()` - 初始化公司默认数据（角色、支付方式、单位、设置）
- `setupDefaultSettings()` - 设置 40+ 项默认配置（见下文）
- `deleteCompany()` - 级联删除公司所有数据

**默认设置项** (`setupDefaultSettings` 方法):

| 分类 | 设置项示例 |
|------|------------|
| 编号生成 | `invoice_number_format`, `estimate_number_format`, `payment_number_format` |
| 日期格式 | `carbon_date_format`, `moment_date_format`, `carbon_time_format` |
| 邮件配置 | `invoice_mail_body`, `estimate_mail_body`, `payment_mail_body` |
| 地址格式 | `invoice_company_address_format`, `invoice_shipping_address_format` |
| 自动化 | `invoice_auto_generate`, `estimate_set_expiry_date_automatically` |
| 本地化 | `currency`, `time_zone`, `language`, `fiscal_year` |
| 功能开关 | `tax_per_item`, `discount_per_item`, `retrospective_edits` |

#### 1.2.2 CompanySetting 模型 (`app/Models/CompanySetting.php`)

**核心 API**:
```php
// 批量设置/更新配置
public static function setSettings($settings, $company_id)

// 获取公司所有配置
public static function getAllSettings($company_id): Collection

// 获取指定配置项
public static function getSettings($settings, $company_id): Collection

// 获取单个配置
public static function getSetting($key, $company_id): mixed
```

**查询作用域**:
```php
public function scopeWhereCompany($query, $company_id)
```

#### 1.2.3 User 模型 (`app/Models/User.php`)

**公司相关方法**:
```php
// 获取用户所属公司
public function companies(): BelongsToMany

// 检查用户是否属于某公司
public function hasCompany($company_id): bool

// 检查用户是否是公司所有者
public function isOwner(): bool
```

### 1.3 数据隔离机制

所有业务模型都通过 `company_id` 字段实现数据隔离：

| 模型 | 隔离方式 |
|------|----------|
| Customer, Invoice, Estimate | 直接包含 `company_id` 字段 |
| Role, Ability | 通过 Bouncer 的 `scope` 字段实现多租户 |
| Media | 通过 Spatie Media Library 的 `model_type` + `model_id` 间接关联 |

---

## 2. 请求上下文解析路径

### 2.1 前端公司上下文管理

#### 2.1.1 前端存储

**localStorage 键值**:
- `selectedCompany` - 当前选中的公司 ID
- `auth.token` - 认证令牌

**Pinia Store** (`resources/scripts/admin/stores/company.js`):
```javascript
state: {
  companies: [],              // 用户所属公司列表
  selectedCompany: null,      // 当前选中公司对象
  selectedCompanySettings: {}, // 当前公司设置缓存
  selectedCompanyCurrency: null // 当前公司货币
}

actions: {
  setSelectedCompany(data) {
    window.Ls.set('selectedCompany', data.id)
    this.selectedCompany = data
  }
}
```

#### 2.1.2 HTTP 请求拦截

**文件**: `resources/scripts/http/index.js`

```javascript
instance.interceptors.request.use(function (config) {
  const companyId = Ls.get('selectedCompany')
  const authToken = Ls.get('auth.token')

  if (authToken) {
    config.headers.Authorization = authToken
  }

  if (companyId) {
    config.headers.company = companyId  // 关键：注入 company header
  }

  return config
})
```

**重要**: 每个 API 请求都会自动携带 `company` 请求头。

#### 2.1.3 公司切换流程

**组件**: `resources/scripts/components/CompanySwitcher.vue`

```javascript
async function changeCompany(company) {
  await companyStore.setSelectedCompany(company)  // 更新 localStorage 和 store
  router.push('/admin/dashboard')                 // 跳转到仪表盘
  await globalStore.setIsAppLoaded(false)
  await globalStore.bootstrap()                   // 重新加载应用配置
}
```

切换公司时会触发完整的应用引导流程，确保所有上下文正确重置。

### 2.2 后端中间件管道

**中间件注册**: `bootstrap/app.php`

```php
->withMiddleware(function (Middleware $middleware) {
    $middleware->alias([
        'auth' => Authenticate::class,
        'bouncer' => ScopeBouncer::class,      // 权限范围
        'company' => CompanyMiddleware::class, // 公司上下文
        // ...
    ]);
})
```

**路由分组**: `routes/api.php:191`

```php
Route::middleware(['auth:sanctum', 'company'])->group(function () {
    Route::middleware(['bouncer'])->group(function () {
        // 所有需要认证和公司上下文的 API 路由
    });
});
```

**执行顺序**:
1. `auth:sanctum` - 认证用户
2. `company` - 解析并验证公司上下文
3. `bouncer` - 设置权限范围

#### 2.2.1 CompanyMiddleware (`app/Http/Middleware/CompanyMiddleware.php`)

**核心逻辑**:
```php
public function handle(Request $request, Closure $next): Response
{
    if (Schema::hasTable('user_company')) {
        $user = $request->user();

        // 验证逻辑：
        // 1. 如果请求头没有 company，或用户不属于该公司
        // 2. 则使用用户的第一个公司作为默认值
        if ((! $request->header('company')) || (! $user->hasCompany($request->header('company')))) {
            $request->headers->set('company', $user->companies()->first()->id);
        }
    }

    return $next($request);
}
```

**安全特性**:
- 防止用户访问不属于自己的公司数据
- 提供优雅的降级策略（默认使用第一个公司）

#### 2.2.2 ScopeBouncer (`app/Http/Middleware/ScopeBouncer.php`)

**权限范围设置**:
```php
public function handle(Request $request, Closure $next): Response
{
    $user = $request->user();
    $tenantId = $request->header('company')
        ? $request->header('company')
        : $user->companies()->first()->id;

    $this->bouncer->scope()->to($tenantId);  // 设置 Bouncer 权限范围

    return $next($request);
}
```

**作用**: 将所有权限检查限制在当前公司范围内，实现多租户权限隔离。

#### 2.2.3 自定义 Scope (`app/Bouncer/Scopes/DefaultScope.php`)

扩展 Bouncer 的默认 Scope 实现：
```php
protected function applyToQuery($query, $table)
{
    return $query->where(function ($query) use ($table) {
        $query->where("{$table}.scope", $this->scope)
            ->orWhereNull("{$table}.scope");  // 支持全局角色/权限
    });
}
```

### 2.3 Bootstrap 流程 - 上下文初始化

**控制器**: `app/Http/Controllers/V1/Admin/General/BootstrapController.php`

每次页面加载或切换公司时调用，返回完整上下文：

```php
public function __invoke(Request $request)
{
    $current_user = $request->user();
    $current_user_settings = $current_user->getAllSettings();
    
    $companies = $current_user->companies;
    $current_company = Company::find($request->header('company'));
    
    // 双重验证：确保用户有权访问该公司
    if ((! $current_company) || ($current_company && ! $current_user->hasCompany($current_company->id))) {
        $current_company = $current_user->companies()->first();
    }
    
    $current_company_settings = CompanySetting::getAllSettings($current_company->id);
    $current_company_currency = Currency::find($current_company_settings->get('currency'));
    
    BouncerFacade::refreshFor($current_user);  // 刷新用户权限缓存
    
    return response()->json([
        'current_user' => new UserResource($current_user),
        'current_user_settings' => $current_user_settings,
        'current_user_abilities' => $current_user->getAbilities(),
        'companies' => CompanyResource::collection($companies),
        'current_company' => new CompanyResource($current_company),
        'current_company_settings' => $current_company_settings,
        'current_company_currency' => $current_company_currency,
        'config' => config('invoiceshelf'),
        'global_settings' => $global_settings,
        'main_menu' => $main_menu,
        'setting_menu' => $setting_menu,
        'modules' => Module::where('enabled', true)->pluck('name'),
    ]);
}
```

### 2.4 公司上下文在代码中的使用模式

#### 2.4.1 控制器中获取当前公司

```php
// 方式 1：从请求头获取
$companyId = $request->header('company');
$company = Company::find($companyId);

// 方式 2：从设置模型获取
$settings = CompanySetting::getAllSettings($request->header('company'));

// 方式 3：授权检查
$this->authorize('manage company', $company);
```

#### 2.4.2 模型中使用公司上下文

```php
// 从请求头获取（依赖全局请求状态）
$dateFormat = CompanySetting::getSetting('carbon_date_format', request()->header('company'));

// 从模型自身的 company_id 获取（推荐）
$dateFormat = CompanySetting::getSetting('carbon_date_format', $this->company_id);
```

#### 2.4.3 查询作用域模式

```php
// User 模型中的公司过滤
public function scopeWhereCompany($query)
{
    return $query->whereHas('companies', function ($q) {
        $q->where('company_id', request()->header('company'));
    });
}
```

---

## 3. 缓存处理情况分析

### 3.1 缓存使用现状

**重要发现**: InvoiceShelf 在公司设置和上下文处理方面**几乎没有使用缓存**。

#### 3.1.1 后端缓存分析

通过代码搜索 (`Cache::`, `cache()`, `->remember()`, `->forget()`)，仅在备份模块中发现缓存使用：
- `app/Http/Controllers/V1/Admin/Backup/BackupsController.php` - 备份列表临时缓存

**公司设置读取路径**（无缓存）:
```
HTTP 请求 → CompanyMiddleware → 控制器 → CompanySetting::getSetting() → 直接 SQL 查询
```

#### 3.1.2 各层读取频率分析

| 层级 | 读取方式 | 缓存情况 |
|------|----------|----------|
| 模型层 | `CompanySetting::getSetting()` | ❌ 每次调用都查询数据库 |
| 控制器 | `CompanySetting::getAllSettings()` | ❌ Bootstrap 时全量查询 |
| 视图层 | 通过 API 响应获取 | ✅ 前端 Pinia Store 缓存 |

#### 3.1.3 典型性能热点

**示例 1: Invoice 模型访问** (`app/Models/Invoice.php`)
```php
public function getFormattedInvoiceDateAttribute($value)
{
    // 每次访问属性都查询数据库
    $dateFormat = CompanySetting::getSetting('carbon_date_format', $this->company_id);
    return Carbon::parse($this->invoice_date)->format($dateFormat);
}
```

如果在列表页显示 50 条发票记录，会产生 **50 次重复查询**。

**示例 2: Bootstrap 接口**
每次调用 `CompanySetting::getAllSettings($companyId)` 都会执行:
```sql
SELECT * FROM company_settings WHERE company_id = ?
```

### 3.2 前端缓存策略

#### 3.2.1 Pinia Store 缓存

`company.js` Store 维护的状态：
- `selectedCompanySettings` - 当前公司设置
- `selectedCompanyCurrency` - 当前公司货币

**更新时机**:
- 调用 `/api/v1/bootstrap` 时全量更新
- 调用 `updateCompanySettings` 时局部更新

#### 3.2.2 localStorage 持久化

仅存储 `selectedCompany`（公司 ID），不缓存设置数据。

### 3.3 切换公司时的缓存处理

#### 3.3.1 前端切换流程

```
用户点击切换公司
    ↓
setSelectedCompany(company) → 更新 localStorage 和 Store
    ↓
跳转到 /admin/dashboard
    ↓
globalStore.bootstrap() → 调用 /api/v1/bootstrap
    ↓
获取新公司的完整上下文 → 替换 Store 中的所有状态
    ↓
后续请求自动携带新的 company header
```

#### 3.3.2 后端处理

**无状态设计**:
- 后端不保存用户当前公司的会话状态
- 完全依赖每个请求的 `company` header
- 切换公司不需要后端做任何缓存清理

#### 3.3.3 权限缓存处理

```php
// BootstrapController.php:50
BouncerFacade::refreshFor($current_user);
```

切换公司时会刷新 Bouncer 的权限缓存，确保权限检查基于新的公司范围。

### 3.4 系统级配置缓存

#### 3.4.1 AppConfigProvider (`app/Providers/AppConfigProvider.php`)

在应用启动时从数据库加载系统级配置：

```php
public function boot(): void
{
    if (! InstallUtils::isDbCreated()) {
        return;
    }

    $this->configureMailFromDatabase();   // 邮件配置
    $this->configurePDFFromDatabase();    // PDF 配置
    $this->configureFileSystemFromDatabase(); // 文件系统配置
}
```

**特点**:
- 仅在框架启动时执行一次
- 配置写入 Laravel 的 `Config` 门面
- 不针对具体公司（使用 `Setting` 模型而非 `CompanySetting`）

### 3.5 缓存优化建议

当前实现存在明显的性能优化空间：

#### 3.5.1 公司设置缓存方案

```php
// 优化后的 CompanySetting 模型
public static function getSetting($key, $company_id)
{
    return cache()->remember("company_setting:{$company_id}:{$key}", 3600, function () use ($key, $company_id) {
        $setting = static::whereOption($key)->whereCompany($company_id)->first();
        return $setting ? $setting->value : null;
    });
}

public static function setSettings($settings, $company_id)
{
    foreach ($settings as $key => $value) {
        cache()->forget("company_setting:{$company_id}:{$key}");
        // ... 原有逻辑
    }
}
```

#### 3.5.2 全量设置缓存

```php
public static function getAllSettings($company_id)
{
    return cache()->remember("company_settings:{$company_id}", 3600, function () use ($company_id) {
        return static::whereCompany($company_id)
            ->get()
            ->mapWithKeys(fn($item) => [$item['option'] => $item['value']]);
    });
}
```

---

## 4. 架构总结

### 4.1 设计优点

1. **清晰的分层架构**: 系统级 → 公司级 → 用户级三级配置隔离
2. **无状态后端**: 依赖请求头传递公司上下文，便于水平扩展
3. **灵活的 KV 存储**: `company_settings` 表支持无限扩展配置项
4. **完善的权限隔离**: Bouncer Scope 确保跨公司数据安全
5. **前端状态管理**: Pinia Store 提供良好的用户体验

### 4.2 潜在问题

1. **N+1 查询风险**: 模型访问器中直接查询配置可能导致性能问题
2. **缺少缓存层**: 高并发场景下数据库压力较大
3. **请求头依赖**: 所有 API 必须正确传递 `company` header
4. **上下文污染**: `request()->header('company')` 全局访问可能导致测试困难

### 4.3 关键文件速查表

| 功能 | 文件路径 |
|------|----------|
| 公司模型 | `app/Models/Company.php` |
| 公司设置模型 | `app/Models/CompanySetting.php` |
| 公司中间件 | `app/Http/Middleware/CompanyMiddleware.php` |
| 权限范围中间件 | `app/Http/Middleware/ScopeBouncer.php` |
| 上下文初始化 | `app/Http/Controllers/V1/Admin/General/BootstrapController.php` |
| 前端公司 Store | `resources/scripts/admin/stores/company.js` |
| 公司切换组件 | `resources/scripts/components/CompanySwitcher.vue` |
| HTTP 拦截器 | `resources/scripts/http/index.js` |
